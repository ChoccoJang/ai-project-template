# AGENTS.md — 공통 규칙

> 이 저장소에서 일하는 AI Agent가 지키는 **공통 규칙의 단일 출처(single source of truth)** 다.
> 도구별 파일(`CLAUDE.md` 등)은 이 내용을 중복하지 않고 가리키기만 한다.
> 규칙이 바뀌면 여기를 고친다. Codex는 이 문서를 직접 읽는다.

## 이 문서를 읽고 쓰는 법

**여기에는 프로젝트와 무관한 고정 규칙만 둔다.** 각 절은 "무엇을 지켜라"를 말한다. 프로젝트가
무엇이고 무엇을 만들며 지금 어떤 모습인지, 그리고 **그 프로젝트에서만 통하는 규칙**이
무엇인지는 담지 않고 그것을 담는 문서를 가리킨다. 그래서 **이 파일은 프로젝트가 고치지
않는다** — 템플릿이 갱신되면 통째로 바꿔 받을 수 있다.

| 알고 싶은 것 | 볼 곳 |
|---|---|
| 이 프로젝트가 무엇인가 | [`README.md`](./README.md) |
| 이 프로젝트에서만 통하는 규칙 (배치·명령·아키텍처·코딩 가이드·검증) | [`.ai/project-rules.md`](./.ai/project-rules.md) |
| 무엇을 만들기로 했고 어디까지 됐나 (= 범위) | [`.ai/status.md`](./.ai/status.md) |
| 지금 무엇을 먼저 하나 | [`.ai/status.md`](./.ai/status.md)의 우선순위 |
| 하고 싶지만 아직 아닌 것 | [`.ai/backlog.md`](./.ai/backlog.md) |
| 지금 어떤 모습인가 (설계·인터페이스 계약) | [`.ai/design/`](./.ai/design/) |
| 왜 그렇게 정했나 | [`.ai/adr/`](./.ai/adr/) |
| 무엇이 아직 안 됐나 | [`.ai/issues/`](./.ai/issues/) |
| 위 문서들을 어떻게 쓰나 (역할·절차·머리말·상태 어휘·Phase 표기) | [`.ai/README.md`](./.ai/README.md) |

**프로젝트마다 있고 없고가 갈리는 것은 여기 두지 않는다.** 언어·프레임워크 컨벤션, 빌드
명령, 개발 가이드·표준 가이드처럼 프로젝트가 바뀌면 함께 바뀌는 것은 전부
[`.ai/project-rules.md`](./.ai/project-rules.md)가 담고, 이 문서의 각 절은 그 자리를 한 줄로
가리킨다. 규칙 문서에 프로젝트를 섞으면 프로젝트가 자랄 때마다 규칙이 흔들린다.

**이 문서를 고치는 것은 소유자가 정한다.** 프로젝트와 무관한 규칙을 바꾸는 것이라 그 자체가
결정이므로 ADR로 남긴다. 에이전트가 임의로 바꾸지 않는다. 반면
[`.ai/project-rules.md`](./.ai/project-rules.md)는 에이전트가 채워도 된다 — 다만 채운 사실을
작업 결과에 남긴다(그 문서의 앞머리).

| 절 | 절 |
|---|---|
| 1. 작업 원칙 | 6. 산출물 규칙 |
| 2. 범위 규칙 | 7. 문서화 규칙 |
| 3. 저장소 배치 | 8. 커밋 / 브랜치 / PR 규칙 |
| 4. 빌드 / 실행 | 9. 검증과 완료 |
| 5. 아키텍처 원칙 | 10. 작업 절차 (체크리스트) |

**도입한 뒤에는 절 번호를 바꾸지 않는다.** 다른 문서와 커밋이 "AGENTS 4절"로 참조한다.
새 규칙이 필요하면 다음 번호로 이어 붙이고, 빈 번호를 미리 예약하지 않는다. 해당 없는 절도
지우지 않고 "해당 없음"으로 남긴다. 번호를 바꿔야 한다면 저장소 전체의 참조를 함께 고친다 —
`check_doc_index.py`가 존재하지 않는 절을 가리키는 참조를 잡는다.

---

## 1. 작업 원칙

- **동작하는 것을 먼저 만든다.** 과도한 추상화보다 실행 가능한 최소 단위를 우선한다.
- **미리 만들지 않는다.** 지금 쓰지 않는 확장·계층·설정은 넣지 않는다. 확장 여지는 구조로
  남기되 구현하지 않는다.
- **의존성을 임의로 늘리지 않는다.** 프레임워크·라이브러리를 새로 들이는 것은 되돌리기
  가장 비싼 결정이라 소유자가 정한다. 표준 라이브러리나 이미 있는 의존성으로 되는지 먼저 본다.
- **기존 것을 먼저 본다.** 파일 구조와 코드·문서 스타일을 확인하고 그것을 따른다. 새 구조를
  만들기 전에 있는 것으로 되는지 확인한다.
- **핵심 흐름을 흐리지 않는다.** 입력에서 결과까지의 경로가 코드에서 그대로 읽혀야 한다.
- **모르면 가정을 밝히거나 묻는다.** 추측으로 채우고 넘어가지 않는다.
- **사용자의 최신 직접 지시가 이 문서보다 우선한다.**
- **끝나면 무엇을 왜 바꿨는지와 실행·검증 방법을 함께 남긴다**(10절).

## 2. 범위 규칙

**범위의 단일 출처는 [`.ai/status.md`](./.ai/status.md)의 구현 현황 표다.** 범위 목록을 여기
적지 않는다.

```text
.ai/backlog.md   →   .ai/status.md 구현 현황 표   →   .ai/status.md 우선순위
  하고 싶다             하기로 했다 (🚧 Not started)          다음에 한다      →  ✅ Done
```

- **구현 현황 표에 없는 것은 하지 않는다.** 작업 중에 필요해 보이는 것이 생겨도 그 자리에서
  만들지 않는다.
- 하고 싶은 것이 생기면 [`.ai/backlog.md`](./.ai/backlog.md)에 적는다. **범위에 넣을지는
  소유자가 정한다** — 에이전트가 스스로 승격시키지 않는다.
- 범위에 넣기로 하면 구현 현황 표에 `🚧 Not started`로 한 줄 추가하고 백로그에서 지운다.
- 범위 밖이지만 고쳐야 할 문제를 발견하면 [`.ai/issues/`](./.ai/issues/)에 남긴다.

## 3. 저장소 배치

### 문서와 CI

```text
저장소 루트/
├── README.md      # 프로젝트 소개·실행 방법 (사용자용)
├── AGENTS.md      # 공통 규칙 (이 파일). Codex가 직접 읽는다
├── CLAUDE.md      # Claude Code 전용 안내
├── .github/       # PR 템플릿, 워크플로, 검사 스크립트
└── .ai/           # AI 에이전트 공용 작업 메모리 (개발용, 배포물 아님)
```

- **개발 과정의 기록은 `.ai/`에만 둔다.** 코드 주석·README·설계 파일에 흩지 않는다.
- **`.ai/`는 배포물이 아니다.** 사용자용 문서와 섞지 않는다. 사용자 문서에서 `.ai/`를
  가리키지 않는다 — 알려진 한계처럼 사용자가 알아야 할 것은 사용자 문서에 직접 적는다.
- 위 다섯 위치는 프로젝트가 바꾸지 않는다.

### 코드 배치

**코드를 어디에 두는지는 [`.ai/project-rules.md`](./.ai/project-rules.md) 3절이 정한다.**

- 단위를 나눴다면 **그 경계를 넘어 코드가 새지 않게** 한다. 하나짜리를 위해 폴더 계층을
  만들지 않는다.

## 4. 빌드 / 실행

**명령의 단일 출처는 [`.ai/project-rules.md`](./.ai/project-rules.md) 4절의 표다.** 명령이
바뀌면 거기를 고치고 다른 문서는 인용만 한다. 에이전트가 가장 먼저 찾는 정보이니 **비워 두지
않는다.**

### 표를 채울 때

- **테스트 명령은 실제로 몇 건을 돌리는지 확인하고 적는다.** 0건을 돌리고 성공으로 끝나는
  명령을 적지 않는다 — 그 뒤로는 아무도 눈치채지 못한다. **쓸 만한 명령이 하나도 없으면**
  차선을 적고 그 한계를 `.ai/issues/`에 남긴다. 명령을 만들려고 산출물을 고치는 것은
  범위 결정이므로 소유자가 정한다(2절).
- 실행에 필요한 설정은 **예시 파일**(`.env.example` 등)로 저장소에 두고, 실제 값은 커밋하지
  않는다.
- **새 의존성을 들이는 것은 소유자가 정한다**(1절).

## 5. 아키텍처 원칙

**바뀌면 안 되는 큰 원칙**은 [`.ai/project-rules.md`](./.ai/project-rules.md) 5절에 적는다.
지금의 설계가 어떤 모습인지는 [`.ai/design/`](./.ai/design/)이, 개별 결정의 배경은
[`.ai/adr/`](./.ai/adr/)이 담는다.

- **원칙을 새로 세우거나 바꾸는 것은 결정이다.** ADR을 함께 쓰고, 소유자가 확인하기 전까지
  그 ADR의 상태를 `Proposed`로 둔다.

## 6. 산출물 규칙

이 저장소의 **산출물**에 적용한다. 무엇을 만드는지는 프로젝트가 정하고(2절), 여기서는
**언어·프레임워크가 무엇이든 지켜야 할 것**만 둔다. 프로젝트별 확장은
[`.ai/project-rules.md`](./.ai/project-rules.md) 6절이 담는다.

### 값과 비밀

- **설정값을 하드코딩하지 않는다.** 포트·경로·URL·자격증명은 설정이나 환경변수로 뺀다.
- **비밀정보를 산출물·로그·커밋에 남기지 않는다.** 토큰·비밀번호·키는 생성하지도 않는다.
  개인정보·인증 토큰·쿠키를 수집하거나 로깅하지 않는다.

### 읽히게 만든다

- **이름은 하는 일을 그대로 말한다.**
- **에러를 삼키지 않는다.** 잡았으면 처리하거나 맥락을 붙여 다시 던진다.
  **바깥 경계에서는 내부 예외를 그대로 노출하지 않는다** — 사용자·호출자에게는 무엇이
  잘못됐는지 읽히는 형태로 알린다.
- **주석은 왜를 적는다.** 무엇을 하는지는 코드가 말하게 한다. **배경이 세 줄을 넘으면
  ADR로 옮기고 링크만 남긴다** — `// 근거: .ai/adr/0007-slug.md`. 코드에 배경을 쌓으면
  결정이 바뀌었을 때 주석만 남아 거짓말을 한다.

### 코드 품질 기준

**언어·프레임워크와 무관하게 지키는 최소선이다.** 여기 있는 것은 어느 프로젝트에서나 성립하고,
그 위의 세부(언어별 컨벤션, 추가 기준, 린터 설정)는
[`.ai/project-rules.md`](./.ai/project-rules.md) 6절이 확장한다. **확장은 이 기준을 완화하는
방향으로 하지 않는다.**

**한 가지 일만 한다**

- **함수·클래스·모듈은 한 가지 일을 한다.** 이름에 "그리고"가 들어가야 설명되면 나눈다.
- **공개하는 것을 최소로 연다.** 기본은 비공개이고, 밖에서 실제로 쓰는 것만 공개한다.
- **전역 가변 상태를 만들지 않는다.** 상태는 넘겨받아 쓰고 소유자를 하나로 둔다.
- **함수 길이와 중첩 깊이는 언어별 린터에 맡긴다** — 문서로 숫자를 정하지 않는다.

**중복과 죽은 코드**

- **같은 코드가 세 번째로 필요할 때 뽑는다.** 두 번째에서 참는다 — 이른 추상화가 중복보다 비싸다.
- **쓰지 않는 코드를 남기지 않는다.** 주석 처리한 코드·도달하지 않는 분기·안 쓰는 인자와
  import는 지운다. 되살릴 것은 git이 갖고 있다.
- **TODO에는 무엇을·왜가 붙는다.** 붙일 수 없으면 `.ai/issues/`로 옮기고 코드에서는 지운다.

**값과 입력**

- **매직 넘버·매직 문자열을 쓰지 않는다.** 이름 있는 상수로 뺀다.
- **바깥에서 온 입력은 경계에서 검증한다.** 안쪽 코드가 이미 검증된 값만 다루게 한다.
- **실패할 수 있는 호출의 결과를 무시하지 않는다.** 반환값·예외 중 하나는 반드시 다룬다.

**자원과 외부 호출**

- **열었으면 닫는다.** 파일·소켓·트랜잭션은 언어가 주는 구조로 스코프 안에서 해제한다.
- **외부 호출에 타임아웃 없이 기다리지 않는다.** 재시도를 넣는다면 횟수와 간격을 정해서 넣는다.
- **로그는 무엇이 일어났는지 읽히게 남긴다.** 비밀정보는 넣지 않는다(위).

**테스트**

- **새 동작에는 테스트를 붙이고, 버그 수정에는 재현 테스트를 먼저 붙인다.**
- **테스트 하나는 한 동작을 확인하고, 이름이 그 동작을 말한다.**
- **시간·난수·네트워크·파일 시스템에 그대로 기대지 않는다** — 고정하거나 대체한다.
- **테스트는 실패할 수 있어야 한다.** 통과시키려고 단언을 지우거나 테스트를 건너뛰지 않는다.

**성능**

- **측정하지 않고 최적화하지 않는다.** 대신 명백한 낭비(루프 안의 재계산, N+1 조회, 불필요한
  전체 복사)는 처음부터 피한다.

### 파일 크기

- **기본 상한은 파일당 400줄이다.** 조정과 예외는
  [`.github/file-size.json`](./.github/file-size.json)이 정하고 CI가 확인한다(9절).
  **상한을 늘리거나 예외를 추가하는 것은 소유자가 정한다** — 검사에 걸렸다고 에이전트가
  기준을 고쳐 통과시키지 않는다. `--update-baseline`도 줄어든 것을 반영할 때만 쓴다.
- **줄 수는 신호이지 목표가 아니다.** 상한에 맞춰 자르지 않는다. 파일 하나는 **한 가지
  개념**을 담고, 나눌 때는 **변경 축**으로 나눈다 — 다른 이유로 바뀌는 코드를 한 파일에
  두지 않는다.
- **새 파일이 무엇을 담는지 한 줄로 먼저 정한다.** 기존 파일에 더하기 전에 그 한 줄에
  맞는지 확인하고, 맞지 않으면 새 파일로 간다.
- **이름을 붙일 수 없으면 아직 나누지 마라.** `utils` · `helpers` · `common` · `misc` ·
  `manager` · `*2` · `*Impl2` · `*_part2` · `*_new`를 쓰지 않는다.
- 이미 상한을 넘는 파일은 **더 늘리지 않는다**(래칫). 손대는 김에 줄이고, 줄인 만큼 기준선이
  내려간다.

### 바꿀 때

- **한 번에 한 가지만 바꾼다.** 리팩터링과 기능 변경을 같은 커밋에 섞지 않는다. 상한 때문에
  파일을 나누는 것도 따로 커밋한다.
- **바깥에 노출하는 것을 바꾸면 계약 문서와 함께 고친다.** API·CLI·파일 형식·스키마가 여기
  해당한다. 계약의 내용은 [`.ai/design/`](./.ai/design/)이 담고, 같은 PR에서 함께 바꾼다.
  깨는 변경은 ADR로 남긴다.

### 프로젝트별 확장

- **언어·프레임워크 컨벤션은 여기 나열하지 않는다.** 따르는 가이드를
  [`.ai/project-rules.md`](./.ai/project-rules.md) 6절에서 가리킨다. 외부 표준을 그대로
  따른다면 링크만 하고 저장소에 옮겨 적지 않는다.
- **자동 검사(린트·포맷)로 강제할 수 있는 것은 문서가 아니라 검사에 넣는다**(4절·9절).

## 7. 문서화 규칙

- **한국어로 쓴다.** 단 코드·인터페이스 명세·필드명·설정명은 영어를 쓴다.
- **왜 필요한지, 어디까지 할지, 예시를 담는다.**
- **상태를 자체 서술하지 않는다.** 진행 상황은 `.ai/status.md`가 단일 출처다.
  **사용자 문서에는 진행 상황을 쓰지 않는다** — `.ai/`를 가리킬 수도 없으므로(3절),
  사용자가 알아야 할 한계만 직접 적는다.

### `.ai/` 문서

**각 문서의 역할·갱신 방식·추가 절차·상태 어휘는 [`.ai/README.md`](./.ai/README.md)가
단일 출처**다. 여기서는 그것을 지키기 위한 규칙만 둔다.

- **파일명은 영문 소문자 슬러그**로 쓴다 — `NNNN-slug.md`(ADR·이슈), `yyyymmdd-slug.md`
  (작업 결과), `{{component}}.md`(설계). 한글·공백·대문자를 쓰지 않는다. 한글 제목은 파일 안의
  H1과 인덱스 표의 링크 텍스트가 담는다.
- **만들면 그 폴더의 `README.md` 인덱스 표에 등록한다.**
- **머리말 필드는 문서마다 다르다.** 무엇을 채워야 하고 그중 어느 것을 CI가 강제하는지는
  `.ai/README.md`의 머리말 필드 표가 정한다. 채울 것이 없는 항목은 비우지 말고
  "해당 없음"으로 적는다.
- **상태 값은 정해진 어휘만 쓴다**(`.ai/README.md`의 상태 어휘 표). 인덱스 표의 상태는 파일
  머리말의 상태와 같아야 한다.
- **덮어쓰는 문서와 추가만 하는 문서를 섞지 않는다.** 설계가 바뀌면 `.ai/design/`을 덮어쓰고
  **왜 바꿨는지는 ADR로 남긴다** — 설계 문서에 "예전에는 …였다"를 적지 않는다.
- **과거 ADR의 본문은 고쳐 쓰지 않는다.** 결정을 뒤집으면 새 ADR을 쓰고 옛 ADR의 상태를
  `Superseded by NNNN`으로 바꾼다.

위 규칙은 `.github/scripts/check_doc_index.py`가 CI에서 확인한다(9절).

### 문서 크기

문서는 **매 세션 읽히는가**로 갈라 다루고, 기준은 `.github/file-size.json`이 정한다.

| 종류 | 규칙 |
|---|---|
| **진입 문서** — `AGENTS.md` · `CLAUDE.md` · `.ai/README.md` | 파일별 상한 없이 **합계 예산**을 지킨다. 모든 세션이 읽는 고정 비용이라 파일을 옮겨 담아도 줄지 않는다 |
| **`.ai/project-rules.md` · `.ai/status.md`** | 자체 상한. 프로젝트와 함께 자라므로 고정 비용이 아니다 — 넘치면 개발 가이드 문서로 밀어내거나(규칙) 끝난 Phase를 접는다(현황) |
| **누적 문서** — 각 폴더의 인덱스 `README.md` · `backlog.md` | 상한 없음. 항목이 쌓이는 것이 정상이다 |
| **개별 기록** — `adr/` · `issues/` · `work-result/` · `design/` | 300줄. 넘으면 한 파일에 여러 건이 섞였다는 뜻이니 나눈다 |

- **진입 문서에는 지켜야 할 것만 적는다.** 각 항목은 한 문장, 근거는 붙여도 한 절까지다.
  그 이상은 원래 자리로 보낸다 — **왜**는 `.ai/adr/`, **어떻게**는 `.ai/README.md`,
  **예시·양식**은 `0000-template.md`, **프로젝트 규칙**은 `.ai/project-rules.md`,
  **언어별 컨벤션**은 그 문서가 가리키는 개발 가이드.
- **예산이 넘치면 규칙을 지우는 것이 아니라 위 다섯 곳으로 밀어낸다.**
- **끝난 Phase는 `.ai/status.md`에서 한 줄로 접는다.** 세부는 `work-result/`가 갖고 있다.
- **인덱스 표가 50줄을 넘으면 상태별로 절을 나눈다**(예: 유효한 것 / 지난 것).

## 8. 커밋 / 브랜치 / PR 규칙

### 브랜치명 / PR 제목

```text
feature/yyyymmdd-work-name
```

- `yyyymmdd`는 브랜치를 만든 날짜, `work-name`은 **영문 소문자**에 하이픈이다.
  한글·공백·대문자를 쓰지 않는다.
- PR 제목은 브랜치명과 같은 값을 쓴다.
- **도구·세션이 이미 만든 브랜치는 그대로 쓴다.** 규칙에 맞추려고 새 브랜치를 만들지
  않는다 — 재명명하면 같은 작업에 브랜치가 둘 생긴다. 위 형식은 **에이전트가 브랜치를
  직접 만들 때** 적용한다.

### 커밋 메시지

```text
type(scope): 한글 요약
```

- `type`과 `scope`는 영문, 요약과 본문은 한글로 쓴다.
- 쓸 수 있는 type: `docs` · `feat` · `fix` · `refactor` · `test` · `chore` · `infra`
- `scope`는 바뀐 대상을 가리키는 짧은 영문 낱말이다(모듈·폴더·문서 이름).

### PR

- **브랜치로 작업한 변경은 PR로 올리고, 리뷰를 받은 뒤 머지한다.** 리뷰 없이 기본
  브랜치에 바로 머지하지 않는다. 리뷰는 둘 중 하나다.
  - **상호 리뷰** — 사람이나 다른 에이전트가 본다. 가능하면 이쪽이다.
  - **자체 리뷰** — 볼 사람이 없을 때. 자기 diff를 처음 보는 사람처럼 다시 읽고, **무엇을
    확인했는지** PR 본문에 남긴다. "확인함"이 아니라 확인한 항목을 적는다.
- **원격이 없거나 소유자가 PR을 쓰지 않기로 했으면 이 항목은 "해당 없음"이다.** 기본
  브랜치에 직접 커밋하고 작업 결과 문서의 `PR` 필드에 사유를 적는다. **PR을 열 수 없다고
  작업을 멈추거나 머지되지 못할 브랜치를 만들지 않는다.**
- **본문은 [`.github/pull_request_template.md`](./.github/pull_request_template.md)를 채워
  쓴다.** API·CLI로 만들 때는 그 파일을 직접 읽어 채운다. 빈 섹션을 두지 않고, 해당 없으면
  "해당 없음"으로 적는다. 안내용 HTML 주석은 지운다.
- **PR을 연 뒤에는 CI·리뷰 반응을 계속 추적하고, 끝나면 뒷정리까지 한다.** 언제 무엇을
  하는지는 10절이, 추적에 쓸 수단은 도구별 파일이 정한다.

### 코멘트 이모지 신호 (머지 / 중단)

소유자가 매번 채팅으로 확인해 주지 않아도 되도록, **소유자가 PR 코멘트로 낸 이모지를 지시로
본다.**

| 신호 | 뜻 | 조치 |
|---|---|---|
| 🚀 | 머지해도 된다 | CI green·리뷰 지적 해소를 함께 확인하고 머지한다 |
| 👎 · ❌ · 🛑 | 머지하지 않는다 | PR을 close한다. 남은 지적은 `.ai/issues/`에 등재한다 |

- **코멘트에서만 읽는다.** 소유자가 쓴 **코멘트 본문의 이모지**와 **코멘트에 달린 반응**,
  이 둘뿐이다. **PR 본문(설명)에 달린 반응은 신호가 아니다** — 볼 자리를 줄여 놓치지 않게
  하고, 설명에 섞인 이모지가 머지를 일으키지 않게 한다.
- **자리마다 쓸 수 있는 기호가 다르다.** 반응으로 달 수 있는 이모지는 여덟 개뿐이라
  **반응 자리에서는 🚀·👎만** 인정하고, **코멘트 본문에서는 ❌·🛑도 중단 신호**로 읽는다.
- **어느 자리든 낸 사람이 소유자**여야 한다.
- **코멘트에 다른 말이 함께 있으면 그 말이 우선한다.** 지적·질문이 섞인 코멘트의 🚀는 승인이
  아니라 "이건 하고 머지하라"는 뜻이고, 사유가 붙은 👎는 그 사유를 먼저 읽는다.
- **그 밖의 이모지는 신호가 아니다.** 실수로 누른 반응이 머지나 close를 일으키지 않게 한다.
- **누가 눌렀는지 볼 수 없는 도구라면** 이 이모지들을 소유자만 쓴다는 전제로 동작한다. 그
  전제가 불확실한 PR(외부 기여자 등)에서는 이모지 대신 소유자의 명시적 코멘트를 확인한다.
- **신호가 없으면 머지도 close도 하지 않는다.** 되돌리기 어렵거나 논쟁 여지가 있는 변경은
  🚀가 있어도 한 번 더 묻는다.
- **어느 신호든 확인한 그 턴 안에서 종료 절차까지 끝낸다**(10절).

### 코드 리뷰

- **코드를 바꾼 작업은 PR을 열기 전에 코드 리뷰를 한 번 돌리고, 지적을 반영한 상태로 연다.**
  리뷰를 PR 뒤로 미루지 않는다 — 열자마자 고칠 것이 나오면 리뷰어의 시간을 두 번 쓴다.
  문서 전용 작업은 해당 없다.
  - 도구에 **코드 리뷰 기능이 있으면 그것으로 돌린다.** 무엇을 쓰는지는 도구별 파일이 정한다.
  - 없으면 위 **자체 리뷰**로 대신한다. 어느 쪽이든 **무엇을 봤는지** PR 본문에 남긴다.
- **리뷰 요청은 한 번이다.** 반영해서 다시 push했다고 리뷰를 자동으로 다시 요청하거나 같은
  diff를 스스로 또 훑지 않는다 — 에이전트가 리뷰 루프를 혼자 돌리지 않는다. **이미 달린 지적을
  반영하는 것과, 변경을 요청한 사람 리뷰어에게 다시 봐 달라고 하는 것은 재요청이 아니다**
  (10절의 추적).
- **한 번으로 부족해 보이면 스스로 반복하지 말고 소유자에게 알린다.** 무엇이 남았고 왜 더
  필요한지 적는다 — 다회 리뷰는 소유자가 별도 작업으로 정한다.
- **리뷰에서 나왔지만 이번에 해결하지 못한 지적은 [`.ai/issues/`](./.ai/issues/)에 등재한다.**
  지적 1건 = 이슈 1건이고, 미루기로 한 것은 `Deferred`, 남은 것은 `Open`이다. PR 본문과 작업
  결과 문서에서 그 이슈를 가리킨다. **머지된다고 지적이 사라지는 것이 아니다** — 다음 작업은
  `.ai/issues/`를 먼저 읽고 시작하되, 그중 무엇을 실제로 할지는 범위가 정한다(2절).

### 브랜치 정리

- **로컬 브랜치는 에이전트가, 원격 브랜치는 소유자가 삭제한다.** 에이전트는
  `git push origin --delete`를 실행하지 않는다.
- **남이 만든 브랜치의 히스토리를 재작성하지 않는다.** rebase·amend·force push로 이미 올라간
  커밋을 지우지 않는다. 자기가 만든 브랜치에서도 남이 그 브랜치를 받아 갔다면 마찬가지다.

### 작업 결과 문서

- 작업이 끝나면 [`.ai/work-result/`](./.ai/work-result/)에 결과를 남긴다.
- **PR 본문과 같은 내용을 두 번 쓰지 않는다.** 섹션별 소유자는 다음과 같다.

  | 내용 | PR 본문 | 작업 결과 문서 |
  |---|---|---|
  | 목적 / 배경 | ✅ | ❌ (머리말 `PR` 필드로 가리킨다) |
  | 변경 내용 | ✅ 리뷰용 상세 | ✅ 파일·모듈별 한 줄 요약 |
  | 검증 · 체크리스트 | ✅ (머지 전 게이트) | ❌ |
  | 작업요청사항 | ❌ | ✅ |
  | 최종정리내용 · 참고사항 · 제안사항 | ❌ | ✅ |

- **PR이 없으면 위 표에서 ❌인 것도 작업 결과 문서가 담는다** — 그때는 이 문서가 유일한
  기록이다.
- **머리말 `PR` 필드는 번호가 아니라 링크로, PR을 연 뒤에 채운다** — 문서를 먼저 커밋해 PR을
  열고, 받은 링크를 적어 같은 브랜치에 한 번 더 커밋한다(순서는
  [`.ai/work-result/README.md`](./.ai/work-result/README.md)).
- PR을 열지 않기로 한 작업이면 그 자리에 사유를 적는다 — `없음(직접 push)` ·
  `없음(커밋 없음)` · `없음(원격 저장소 없음)`처럼 **무엇 때문인지 드러나게** 쓴다. 이때는 변경 내용에 근거까지 남긴다 — 이 문서가 유일한 기록이다.
- **파일을 하나라도 바꿨으면 남기고, 읽기만 한 작업은 남기지 않는다.**

## 9. 검증과 완료

### 문서 검사

아래 셋은 **프로젝트와 무관하게 항상 돈다.** 이 문서 세트 자체가 지켜지는지 보는 장치라
언어·프레임워크·CI 도구가 무엇이든 그대로 쓴다. **Python 3.11 이상**이 필요하고 표준
라이브러리만 쓴다 — 설치할 패키지는 없다.

```bash
python3 .github/scripts/check_markdown_links.py
python3 .github/scripts/check_doc_index.py
python3 .github/scripts/check_file_size.py
```

- **지우거나 건너뛰지 않는다.** 실패하면 검사를 고치는 것이 아니라 원인을 고친다. 검사
  자체를 바꾸는 것은 규칙을 바꾸는 것이므로 소유자가 정한다.
- **CI에 반드시 건다.** 이 저장소는 [`checks.yml`](./.github/workflows/checks.yml)에 걸어
  두었다. **빌드·테스트를 CI에 어디까지 걸지는 프로젝트가 정한다**
  ([`.ai/project-rules.md`](./.ai/project-rules.md) 9절).
- **로컬에서 먼저 돌린다.** CI는 최종 확인이지 첫 실행 수단이 아니다.
- **재실행을 반복해 디버깅하지 않는다.** 원인을 로컬에서 재현해 고친 뒤 한 번만 다시 돌린다.
- **검사를 늘릴 때 워크플로를 늘리지 말고 기존 워크플로에 step으로 더한다.** CI 도구별
  운영 규칙(트리거·타임아웃·동시성)은 [`.github/workflow-template.yml`](./.github/workflow-template.yml)의
  주석이 담는다.

### 완료 조건

**2는 산출물을 바꾼 작업에만 적용한다.** 문서·조사 작업은 1·3·4를 본다.

1. **위 문서 검사 셋이 통과한다.** 어느 작업에나 해당한다. 산출물을 바꿨으면 4절의
   **빌드·테스트·린트** 명령도 통과해야 한다(설치·실행처럼 통과 개념이 없는 것은 뺀다).
2. **바뀐 것을 확인할 수단이 남아 있다.** 다음 사람이 같은 것을 손으로 다시 확인하지 않아도
   되게 한다. 무엇을 어떻게 확인하는지는 `.ai/project-rules.md` 9절이 정한다.
   **그 절이 비어 있거나 "해당 없음"이면 소유자가 확인해야 완료다** —
   에이전트가 스스로 닫지 않는다. 확인 전까지는 구현 현황 표의 해당 행을 `🟡 In progress`로
   두고, 표에 행이 없는 작업(리팩터링·의존성 갱신 등)이면 **작업 결과 문서에 무엇을 확인받아야
   하는지 적는다** — 행을 새로 만들지 않는다(2절).
3. `.ai/status.md`의 구현 현황 표를 갱신했다(근거로 ADR·PR 링크를 함께 남긴다).
   **표에 있는 항목을 건드린 작업에만 해당한다** — 결정·조사·문서 작업처럼 구현 항목이
   아닌 것은 표에 행을 새로 만들지 않는다. 범위 승격은 소유자가 정한다(2절).
4. 설계가 바뀌었으면 `.ai/design/`을, 결정이 있었으면 `.ai/adr/`을, **리뷰에서 남은
   지적이 있으면 `.ai/issues/`를** 갱신했다.

## 10. 작업 절차 (체크리스트)

9절이 **무엇이 되어야 끝인가**를 정한다면, 이 절은 **어떤 순서로 하는가**를 정한다. 절차가
산문에 흩어져 있으면 도구마다 빠뜨리는 단계가 달라지므로 여기 한 줄씩 모아 둔다. **순서와
항목은 도구와 무관하게 같고, 수단(코드 리뷰 기능·구독·예약 같은 도구 기능)만 도구별 파일이
정한다.**

**작업을 끝내는 마지막 응답에 아래 한 줄을 그대로 옮겨 적고 각 항목의 결과를 채운다.**
항목을 지우거나 줄이지 않는다 — 해당 없으면 `해당 없음`과 사유를 적는다. 스스로 답을 적게
하는 것이 절차가 조용히 빠지는 것을 막는 유일한 장치다.

```text
[작업 절차] 시작확인: / 리뷰: / 검사: / 작업결과+PR링크: / 추적 걸기: / 뒷정리:
```

### 시작할 때

1. `.ai/status.md`의 우선순위와 `.ai/issues/`를 읽고, 할 일이 범위 안인지 확인한다(2절).
2. **이전에 연 PR이 아직 열려 있으면 그것부터 처리한다.** 지시가 없어도 한다 — 세션이 끝나면
   걸어 둔 추적 수단도 함께 죽는다고 가정하고, 아래 6번을 다시 건다.

### PR을 열 때

3. **코드 리뷰를 한 번 돌리고 지적을 반영한다**(8절). 문서 전용 작업은 해당 없다.
4. **문서 검사 셋과 완료 조건을 로컬에서 통과시킨다**(9절).
5. **`.ai/work-result/`에 결과 문서를 커밋해 PR을 열고, 받은 링크를 `PR` 필드에 채워 같은
   브랜치에 한 번 더 커밋한다**(8절). 링크를 채우는 커밋을 잊으면 CI가 빨갛게 남는다.
6. **PR을 연 그 턴 안에서, 턴을 끝내기 전에 추적을 건다.** 수단은 도구별 파일이 정한다.
   - **수단이 도구 오류로 걸리지 않으면** 재시도하고, 그래도 안 되면 그 사실을 PR에 한 줄
     코멘트로 남긴다. 추적이 조용히 끊기는 것이 최악이다.
   - **애초에 그런 수단이 없는 도구라면** 그 사실을 PR에 한 줄 코멘트로 남기고, 대신 **매 턴
     시작 때 그 PR의 상태를 직접 확인한다**(2번). 수단이 없다는 것은 추적하지 않아도 된다는
     뜻이 아니다.

### 추적할 때 — PR이 열려 있는 동안

7. **머지/close 여부 → CI 상태 → 리뷰 코멘트 → 소유자의 이모지 신호**(8절) 순으로 확인한다.
   이모지는 **코멘트 두 자리를 본다** — 코멘트에 달린 반응과 소유자가 쓴 코멘트 본문(8절).
8. 확인한 것에 조치한다: 리뷰 지적 반영과 스레드 resolve, CI 수리, 🚀 + green이면 머지,
   👎면 close.
9. **조치 후에도 PR이 열려 있으면 다음 확인을 다시 걸고 턴을 끝낸다.** "변화 없음"은 재예약을
   건너뛸 사유가 아니라 조용히 재예약할 사유다 — 소유자를 부르지 않는다.

### 끝낼 때 — 머지되었거나 close되었을 때

10. **그 턴 안에서 뒷정리를 끝낸다. 소유자가 말하지 않아도 한다.**
    - **걸어 둔 추적을 전부 해제한다** — 구독·예약 어느 것도 남기지 않는다.
    - **로컬 브랜치를 정리한다.** 원격 브랜치는 지우지 않는다(8절).
    - `.ai/status.md`와 작업 결과 문서가 최종 상태를 반영하는지 확인한다(9절).
    - **남은 지적·미해결은 `.ai/issues/`에 등재한다.** close된 PR의 지적도 사라지지 않는다.
11. **추적을 멈추는 조건은 셋뿐이다: 머지됨 · close됨 · 소유자의 중지 지시.** 그 밖의 이유로
    추적을 그만두지 않는다.
