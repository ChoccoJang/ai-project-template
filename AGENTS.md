# AGENTS.md — 공통 규칙

> 이 저장소에서 일하는 AI Agent가 지키는 **공통 규칙의 단일 출처(single source of truth)** 다.
> 도구별 파일(`CLAUDE.md` 등)은 이 내용을 중복하지 않고 가리키기만 한다.
> 규칙이 바뀌면 여기를 고친다. Codex는 이 문서를 직접 읽는다.

## 이 문서를 읽고 쓰는 법

**여기에는 규칙만 둔다.** 각 절은 "무엇을 지켜라"를 말한다. 프로젝트가 무엇이고 무엇을
만들며 지금 어떤 모습인지는 담지 않고, 그것을 담는 문서를 가리킨다.

| 알고 싶은 것 | 볼 곳 |
|---|---|
| 이 프로젝트가 무엇인가 | [`README.md`](./README.md) |
| 무엇을 만들기로 했고 어디까지 됐나 (= 범위) | [`.ai/status.md`](./.ai/status.md) |
| 지금 무엇을 먼저 하나 | [`.ai/status.md`](./.ai/status.md)의 우선순위 |
| 하고 싶지만 아직 아닌 것 | [`.ai/backlog.md`](./.ai/backlog.md) |
| 지금 어떤 모습인가 (설계·인터페이스 계약) | [`.ai/design/`](./.ai/design/) |
| 왜 그렇게 정했나 | [`.ai/adr/`](./.ai/adr/) |
| 무엇이 아직 안 됐나 | [`.ai/issues/`](./.ai/issues/) |
| **위 문서들을 어떻게 쓰나** (역할·추가 절차·머리말 필드·상태 어휘) | [`.ai/README.md`](./.ai/README.md) |

절은 두 종류다.

| 표시 | 뜻 | 누가 고치나 |
|---|---|---|
| (표시 없음) | **고정 절.** 프로젝트와 무관한 규칙이라 그대로 쓴다. | **소유자 승인이 필요하다.** 고치는 것 자체가 결정이므로 ADR로 남긴다. 에이전트가 임의로 바꾸지 않는다. |
| 📝 | **프로젝트 절.** 형식을 강제하지 않으니 필요한 만큼만 쓰고, 해당 없으면 "해당 없음" 한 줄로 둔다. 채우면서 **안내용 HTML 주석은 지운다.** | 에이전트가 채워도 된다. 다만 **채운 사실을 작업 결과에 남긴다** — 규칙 문서가 바뀐 것이다. |

| 절 | | 절 | |
|---|---|---|---|
| 1. 작업 원칙 | | 9. Phase 규칙 | |
| 2. 범위 규칙 | | 10. 문서화 규칙 | |
| 3. 저장소 배치 | 📝 일부 | 11. 커밋 / 브랜치 / PR 규칙 | |
| 4. 빌드 / 실행 | 📝 | 12. CI / GitHub Actions 규칙 | |
| 5. 아키텍처 원칙 | 📝 | 13. 역할 분담 | 📝 일부 |
| 6. 코딩 규칙 | 📝 일부 | 14. 하지 않는 것 | 📝 일부 |
| 7. 데이터 규칙 | 📝 일부 | 15. 완료 기준 | 📝 일부 |
| 8. 인터페이스 계약 규칙 | | | |

**절 번호는 바꾸지 않는다.** 다른 문서와 커밋이 "AGENTS 4절"로 참조한다. 새 규칙이 필요하면
다음 번호로 이어 붙이고, 빈 번호를 미리 예약하지 않는다. 해당 없는 절도 지우지 않는다.

---

## 1. 작업 원칙

- **동작하는 것을 먼저 만든다.** 과도한 추상화보다 실행 가능한 최소 단위를 우선한다.
- **미리 만들지 않는다.** 지금 쓰지 않는 확장·계층·설정은 넣지 않는다. 확장 여지는 구조로
  남기되 구현하지 않는다.
- **기존 것을 먼저 본다.** 파일 구조와 코드·문서 스타일을 확인하고 그것을 따른다. 새 구조를
  만들기 전에 있는 것으로 되는지 확인한다.
- **핵심 흐름을 흐리지 않는다.** 입력에서 결과까지의 경로가 코드에서 그대로 읽혀야 한다.
- **모르면 가정을 밝히거나 묻는다.** 추측으로 채우고 넘어가지 않는다.
- **끝나면 무엇을 왜 바꿨는지와 실행·검증 방법을 함께 남긴다.**

## 2. 범위 규칙

**범위의 단일 출처는 [`.ai/status.md`](./.ai/status.md)의 구현 현황 표다.** 범위 목록을 여기
적지 않는다 — 범위는 정할 때마다 바뀌고 규칙은 드물게 바뀐다.

```text
.ai/backlog.md   →   .ai/status.md 구현 현황 표   →   .ai/status.md 우선순위
  하고 싶다             하기로 했다 (🚧 Not started)          다음에 한다      →  ✅ Done
```

- **구현 현황 표에 없는 것은 하지 않는다.** 작업 중에 필요해 보이는 것이 생겨도 그 자리에서
  만들지 않는다.
- 하고 싶은 것이 생기면 [`.ai/backlog.md`](./.ai/backlog.md)에 적는다. **범위에 넣을지는
  소유자가 정한다** — 에이전트가 스스로 승격시키지 않는다.
- 범위에 넣기로 하면 구현 현황 표에 `🚧 Not started`로 한 줄 추가하고 백로그에서 지운다.
- 범위 밖이지만 고쳐야 할 문제를 발견하면 [`.ai/issues/`](./.ai/issues/)에 남긴다.

## 3. 저장소 배치

### 고정 — 문서와 CI

```text
저장소 루트/
├── README.md      # 프로젝트 소개·실행 방법 (사용자용)
├── AGENTS.md      # 공통 규칙 (이 파일). Codex가 직접 읽는다
├── CLAUDE.md      # Claude Code 전용 안내
├── .github/       # PR 템플릿, 워크플로, 검사 스크립트
└── .ai/           # AI 에이전트 공용 작업 메모리 (개발용, 배포물 아님)
```

- **개발 과정의 기록은 `.ai/`에만 둔다.** 코드 주석·README·설계 파일에 흩지 않는다.
- **`.ai/`는 배포물이 아니다.** 사용자용 문서(제품 README, 공개 가이드)와 섞지 않는다.
- 위 다섯 위치는 프로젝트가 바꾸지 않는다.

### 📝 프로젝트 — 코드 배치

<!--
  코드를 어디에 두는지 적는다. 트리를 그려도 되고 문장 몇 줄이어도 된다.
  나누는 단위(모듈·서비스·패키지)와 그 이름은 프로젝트가 정한다.
-->

{{코드를 어디에 두는지. 예: 단일 애플리케이션이면 루트에 그대로 두고 별도 구분을 두지 않는다.}}

- 단위를 나눴다면 **그 경계를 넘어 코드가 새지 않게** 한다.
- 나누지 않았다면 나누지 않은 채로 둔다 — 하나짜리를 위해 폴더 계층을 만들지 않는다.

## 4. 📝 빌드 / 실행

에이전트가 가장 먼저 찾는 정보다. **명령의 단일 출처는 이 표다** — 명령이 바뀌면 여기를
고치고, 다른 문서는 인용만 한다. **비워 두지 않는다.**

| 목적 | 명령 |
|---|---|
| 의존성 설치 | `{{설치-명령}}` |
| 빌드 | `{{빌드-명령}}` |
| 테스트 | `{{테스트-명령}}` |
| 로컬 실행 | `{{실행-명령}}` |
| 린트 / 포맷 | `{{린트-명령}}` |

<!-- 해당 없는 행은 명령 자리에 "해당 없음"을 적고, 필요한 행은 추가한다. -->

{{런타임·도구 버전과 실행 전 준비물(환경변수, 로컬 의존 서비스 등). 없으면 "해당 없음".}}

- 실행에 필요한 설정은 **예시 파일**(`.env.example` 등)로 저장소에 두고, 실제 값은 커밋하지
  않는다.

## 5. 📝 아키텍처 원칙

**바뀌면 안 되는 큰 원칙**을 적는다. 지금의 설계가 어떤 모습인지는
[`.ai/design/`](./.ai/design/)이, 개별 결정의 배경은 [`.ai/adr/`](./.ai/adr/)이 담는다.

<!--
  여기 적는 것은 "이 프로젝트에서는 절대 이렇게 하지 않는다"에 해당하는 것들이다.
  개수·형식을 강제하지 않는다. 아직 없으면 "해당 없음"으로 두고, 생길 때 추가한다.
  원칙을 새로 세우거나 바꿀 때는 그 자체가 결정이므로 ADR로 남긴다.
-->

{{아키텍처 불변 원칙. 없으면 "해당 없음".}}

## 6. 코딩 규칙

### 고정 — 언어와 무관한 것

- **설정값을 하드코딩하지 않는다.** 포트·경로·URL·자격증명은 설정이나 환경변수로 뺀다.
- **비밀정보를 코드·로그·커밋에 남기지 않는다.** 토큰·비밀번호·키는 생성하지도 않는다.
- **이름은 하는 일을 그대로 말한다.** 축약어는 저장소에서 이미 쓰는 것만 쓴다.
- **에러를 삼키지 않는다.** 잡았으면 처리하거나 맥락을 붙여 다시 던진다.
- **주석은 왜를 적는다.** 무엇을 하는지는 코드가 말하게 한다.
- **테스트는 실패할 수 있어야 한다.** 통과시키려고 단언을 지우거나 테스트를 건너뛰지 않는다.
- **한 번에 한 가지만 바꾼다.** 리팩터링과 기능 변경을 같은 커밋에 섞지 않는다.

### 📝 프로젝트 — 코딩 가이드

언어·프레임워크별 컨벤션은 **이 문서에 나열하지 않고 별도 가이드 문서를 두고 가리킨다.**
언어가 늘거나 컨벤션이 바뀔 때 규칙 문서를 건드리지 않기 위해서다.

| 적용 대상 | 따르는 가이드 |
|---|---|
| {{적용 대상}} | {{가이드 문서 경로 또는 외부 표준 링크}} |

- 가이드가 아직 없으면 "해당 없음"으로 두고 위 고정 규칙만 적용한다.
- 외부 표준을 그대로 따른다면 그 문서를 링크하고 저장소에 옮겨 적지 않는다.
- 자동 검사(린트·포맷)로 강제할 수 있는 것은 문서가 아니라 **검사에 넣는다**(4절·12절).

## 7. 데이터 규칙

### 고정

- **민감정보는 수집·로깅하지 않는다.** 개인정보·인증 토큰·쿠키·자격증명이 여기 해당한다.
- **표현 규칙은 한 번 정하면 저장소 전체에서 일관되게 쓴다.** 같은 값을 곳에 따라 다르게
  표현하지 않는다.
- **예외를 두려면 아래 셋을 항상 함께 만족시킨다.** 설정하지 않으면 켜지지 않는다.
  1. **옵트인.** 기본은 꺼져 있다.
  2. **기본 마스킹.** 켜더라도 값은 `***`로 기록한다. 실제 값을 보려면 마스킹을 따로 꺼야
     하고, 그 옵트아웃으로 노출되는 데이터의 책임은 운영자가 진다.
  3. **상한.** 길이·건수 상한을 두어 레코드 하나가 비대해지지 않게 한다.

### 📝 프로젝트 — 표현과 정책

<!--
  시간·식별자·금액·좌표 등 프로젝트가 실제로 쓰는 값의 표현을 정한다.
  다루는 데이터가 무엇인지의 서술은 여기가 아니라 .ai/design/이 담는다.
-->

{{표현 규칙과 보관 정책. 예: 시간은 epoch milliseconds, 식별자는 서버가 만드는 UUID.
다루는 데이터가 없으면 "해당 없음".}}

## 8. 인터페이스 계약 규칙

외부에 노출하는 인터페이스(API·CLI·메시지·파일 형식)를 다루는 규칙이다. **계약의 내용은
[`.ai/design/`](./.ai/design/)의 해당 문서**가 담는다 — 계약은 설계이지 규칙이 아니다.

- **제공하는 쪽이 문서화한다.** 소비하는 쪽은 확정된 계약만 쓰고, 같은 계산을 다시 구현하지
  않는다.
- **제공자와 소비자가 나뉘면 합의와 문서화가 구현보다 먼저다.**
- **문서 없이 계약을 바꾸지 않는다.** 설계 문서와 코드를 같은 PR에서 함께 고친다.
- **깨는 변경(breaking change)은 ADR로 남긴다.** 누가 무엇을 맞춰야 하는지 함께 적는다.
- 노출 인터페이스가 없으면 이 절은 지키지 않아도 된다.

## 9. Phase 규칙

작업을 여러 단계로 나눌 때 쓰는 **표기**를 정한다. 지금 어느 Phase인지 같은 상태는 여기 적지
않는다 — `.ai/status.md`가 단일 출처다.

| 구분 | 형식 | 예 |
|---|---|---|
| Phase (대분류) | 대문자 알파벳 한 글자 | `Phase A`, `Phase B` |
| 소분류 | `대문자-숫자` | `A-1`, `A-2`, `B-1` |

- Phase는 `A`부터 순서대로 붙인다. 소분류 번호는 각 Phase 안에서 `1`부터 센다.
- **분리는 소분류까지만 한다.** `A-1-1` 같은 3단계 표기를 쓰지 않는다. 소분류 하나가 너무
  크면 다음 소분류로 쪼개거나(`A-2`) 본문의 체크리스트·항목으로 푼다.
- **한 번 부여한 문자와 번호는 재사용하지 않는다.** 항목이 없어져도 재번호하지 않고 다음
  번호로 이어 붙인다(절 번호와 같은 이유 — 다른 문서와 커밋이 참조한다).
  단 **아직 다른 문서·커밋·브랜치가 참조하지 않은 번호는 정리해도 된다** — 문서를 처음
  채우면서 임시로 붙인 번호까지 영구히 묶어 둘 이유는 없다.
- **첫 Phase는 `A` 하나로 시작한다.** 소유자가 단계를 나눠 주지 않았다면 에이전트가 Phase를
  여러 개로 쪼개지 않는다. `B`를 새로 여는 것은 범위 결정이므로 소유자가 정한다(2절).
  소분류는 필요해질 때 `A-1`부터 이어 붙인다.
- 쓰는 곳: `.ai/status.md`의 현재 Phase·구현 현황 표·우선순위, 기록 문서의 `Phase` 머리말
  필드(10절), 브랜치명(11절 — 영문 소문자이므로 `a-1`로 쓴다).

## 10. 문서화 규칙

- **한국어로 쓴다.** 단 코드·인터페이스 명세·필드명·설정명은 영어를 쓴다.
- **왜 필요한지, 어디까지 할지, 예시를 담는다.**
- **상태를 자체 서술하지 않는다.** 진행 상황은 `.ai/status.md`를 인용한다.

### `.ai/` 문서

개발 과정의 기록은 모두 `.ai/`에 남긴다. **각 문서의 역할·갱신 방식·추가 절차·상태 어휘는
[`.ai/README.md`](./.ai/README.md)가 단일 출처**이고, 여기서는 그것을 지키기 위한 규칙만 둔다.

- **파일명은 영문 소문자 슬러그**로 쓴다 — `NNNN-slug.md`(ADR·이슈), `yyyymmdd-slug.md`
  (작업 결과), `{{component}}.md`(설계). 한글·공백·대문자를 쓰지 않는다. 한글 제목은 파일 안의
  H1과 인덱스 표의 링크 텍스트가 담는다.
- **만들면 그 폴더의 `README.md` 인덱스 표에 등록한다.**
- **머리말 필드는 문서마다 다르다.** 무엇을 채워야 하고 그중 어느 것을 CI가 강제하는지는
  `.ai/README.md`의 머리말 필드 표가 정한다. 채울 것이 없는 항목은 비우지 말고
  "해당 없음"으로 적는다.
- **상태 값은 정해진 어휘만 쓴다**(`.ai/README.md`의 상태 어휘 표). 인덱스 표의 상태는 파일
  머리말의 상태와 같아야 한다.
- **덮어쓰는 문서와 추가만 하는 문서를 섞지 않는다.** 설계가 바뀌면 `.ai/design/`을 덮어쓰고
  **왜 바꿨는지는 ADR로 남긴다** — 설계 문서에 "예전에는 …였다"를 적지 않는다.
- **과거 ADR의 본문은 고쳐 쓰지 않는다.** 결정을 뒤집으면 새 ADR을 쓰고 옛 ADR의 상태를
  `Superseded by NNNN`으로 바꾼다.

위 규칙은 `.github/scripts/check_doc_index.py`가 CI에서 확인한다(12절).

## 11. 커밋 / 브랜치 / PR 규칙

### 브랜치명 / PR 제목

```text
Claude Code: claude/yyyymmdd-work-name
Codex:       codex/yyyymmdd-work-name
```

- `yyyymmdd`는 브랜치를 만든 날짜, `work-name`은 **영문 소문자**에 하이픈이다. 한글·공백·
  대문자를 쓰지 않는다 — 한글 브랜치명은 터미널·CI·URL에서 인코딩된 형태로 보인다.
- PR 제목은 브랜치명과 같은 값을 쓴다.
- **도구·세션이 이미 만든 `claude/*`·`codex/*` 브랜치는 그대로 쓴다.** 날짜가 없거나 임의
  접미사가 붙어 있어도 규칙에 맞추려고 새 브랜치를 만들지 않는다 — 재명명하면 같은 작업에
  브랜치가 둘 생긴다. 위 형식은 **에이전트가 브랜치를 직접 만들 때** 적용한다.

### 커밋 메시지

```text
type(scope): 한글 요약
```

- `type`과 `scope`는 영문, 요약과 본문은 한글로 쓴다.
- 쓸 수 있는 type: `docs` · `feat` · `fix` · `refactor` · `test` · `chore` · `infra`
- `scope`는 바뀐 대상을 가리키는 짧은 영문 낱말이다(모듈·폴더·문서 이름).

### PR

- **브랜치로 작업한 변경은 PR로 올리고, 리뷰를 받은 뒤 머지한다.** 리뷰 없이 기본
  브랜치에 바로 머지하지 않는다.
- **원격 저장소가 없거나 소유자가 PR을 쓰지 않기로 한 프로젝트는 이 절의 PR 항목이
  "해당 없음"이다.** 그때는 기본 브랜치에 직접 커밋하고, 작업 결과 문서의 `PR` 필드에
  그 사유를 적는다. **PR을 열 수 없다는 이유로 작업을 멈추거나, 머지되지 못할 브랜치를
  만들어 두지 않는다.**
- **본문은 [`.github/pull_request_template.md`](./.github/pull_request_template.md)를 채워
  쓴다.** 웹 UI는 자동으로 불러오지만 API·CLI로 만들 때는 그 파일을 직접 읽어 채운다.
  빈 섹션을 두지 않고, 해당 없으면 "해당 없음"으로 적는다. 안내용 HTML 주석은 지운다.
- **PR을 연 뒤에는 CI·리뷰 반응을 계속 추적한다.** 지적사항을 반영하면 해당 리뷰 스레드를
  resolve하고, CI가 green인 것을 확인한 뒤 머지한다. 추적 수단은 도구별 파일이 정한다
  (Claude Code는 [`CLAUDE.md`](./CLAUDE.md)).

### 브랜치 정리

- **로컬 브랜치는 에이전트가, 원격 브랜치는 소유자가 삭제한다.** 에이전트는
  `git push origin --delete`를 실행하지 않는다.

### 작업 결과 문서

- 작업이 끝나면 [`.ai/work-result/`](./.ai/work-result/)에 결과를 남긴다.
- **PR 본문과 같은 내용을 두 번 쓰지 않는다.** 섹션별 소유자는 다음과 같다.

  | 내용 | PR 본문 | 작업 결과 문서 |
  |---|---|---|
  | 목적 / 배경 | ✅ | ❌ (머리말 `PR` 필드로 가리킨다) |
  | 변경 내용 | ✅ 리뷰용 상세 | ✅ 파일·모듈별 한 줄 요약 |
  | 검증 · 체크리스트 | ✅ (머지 전 게이트) | ❌ |
  | 작업요청사항 | ❌ | ✅ |
  | 최종정리내용 · 참고사항 · 제안사항 | ❌ | ✅ |

- PR이 없는 작업이면 머리말 `PR` 필드에 그 사유를 적는다 — `없음(직접 push)` ·
  `없음(커밋 없음)` · `없음(원격 저장소 없음)`처럼 **무엇 때문인지 드러나게** 쓴다. 이때는 변경 내용에 근거까지 남긴다 — 이 문서가 유일한 기록이다.
- **기준은 커밋 여부가 아니라 저장소 파일이 바뀌었는가다.** 파일을 하나라도 바꿨으면 남기고,
  읽기만 한 작업(조사·검토·질문 답변)은 남기지 않는다.

## 12. CI / GitHub Actions 규칙

Actions 실행 분(minute)은 **유료 공유 예산**이다. 워크플로는 "필요할 때만, 짧게" 돌게 만든다.

- **새 워크플로는 [`.github/workflow-template.yml`](./.github/workflow-template.yml)을 복사해
  만든다.** 템플릿의 가드레일(트리거 최소화·concurrency·timeout·최소 permissions)은 지우지
  않는다.
- **트리거를 최소화한다.** `push`와 `pull_request`를 같은 브랜치에 겹치게 걸지 않는다
  (같은 커밋이 두 번 돈다). `push`는 기본 브랜치로 한정하고 `branches: ["**"]`는 쓰지 않는다.
  `paths` 필터를 반드시 건다.
- **`timeout-minutes`를 모든 job에 명시한다**(기본 10분 이하 권장). 지정하지 않으면 360분이라
  걸린 잡 하나가 월 예산을 태울 수 있다.
- **`concurrency` + `cancel-in-progress: true`를 건다.** 연속 push 시 이전 실행을 취소한다.
- **`schedule`(크론)과 매트릭스 빌드는 소유자가 명시적으로 승인할 때만** 추가한다.
- **검사를 늘릴 때 워크플로를 늘리지 말고 기존 워크플로에 step으로 더한다.**
- **로컬에서 먼저 돌린다.** CI는 최종 확인이지 첫 실행 수단이 아니다. 문서를 고쳤으면 아래
  둘을 로컬에서 통과시킨 뒤 올린다.

  ```bash
  python3 .github/scripts/check_markdown_links.py
  python3 .github/scripts/check_doc_index.py
  ```

- **재실행을 반복해 디버깅하지 않는다.** 원인을 로컬에서 재현해 고친 뒤 한 번만 다시 돌린다.
- **사용량 경고가 보이면**(예: 월 예산 80~90% 도달) 워크플로 추가와 트리거 확대를 멈추고
  소유자와 상의한다.

## 13. 역할 분담

### 고정

- **사용자의 최신 직접 지시가 이 절보다 항상 우선한다.**
- **다른 에이전트의 담당 영역은** 사용자의 명시적 지시가 있거나 빌드·계약 일관성을 위한 최소
  변경일 때만 건드리고, 이유와 영향 파일을 작업 결과에 남긴다.
- 공용 인터페이스 계약은 제공하는 쪽이 책임지고 문서화한다(8절).

### 📝 프로젝트 — 담당 배정

<!--
  에이전트를 하나만 쓰면 "해당 없음"으로 둔다.
  여럿이면 누가 어느 경로를 맡는지 적는다. 경로가 겹치지 않게 나눈다.
-->

{{담당 배정. 예: Claude Code는 서버 코드와 배포 스크립트, Codex는 웹 코드.
에이전트가 하나면 "해당 없음".}}

## 14. 하지 않는 것

### 고정

1. 범위(2절 — `.ai/status.md` 구현 현황 표)를 넘는 기능을 임의로 포함
2. 대규모 프레임워크·라이브러리를 소유자 승인 없이 추가
3. 문서 없이 인터페이스 계약 변경(8절)
4. 개인정보·인증 토큰·쿠키 등 민감정보 수집·로깅
5. 포트·경로·자격증명 하드코딩
6. 테스트를 건너뛰거나 지워서 통과시키기
7. 원격 브랜치 삭제, 남의 브랜치 히스토리 재작성(force push)

### 📝 프로젝트 — 추가로 금지할 것

{{이 프로젝트에서 하지 않기로 한 것. 없으면 "해당 없음".}}

## 15. 완료 기준

### 고정

1. 4절의 빌드·테스트 명령이 통과한다.
2. 4절의 실행 방법으로 로컬에서 동작을 확인할 수 있다.
3. 바뀐 동작에 대한 테스트가 있다.
4. `.ai/status.md`의 구현 현황 표를 갱신했다(근거로 ADR·PR 링크를 함께 남긴다).
   **표에 있는 항목을 건드린 작업에만 해당한다** — 결정·조사·문서 작업처럼 구현 항목이
   아닌 것은 표에 행을 새로 만들지 않는다. 범위 승격은 소유자가 정한다(2절).
5. 설계가 바뀌었으면 `.ai/design/`을, 결정이 있었으면 `.ai/adr/`을 갱신했다.

### 📝 프로젝트 — 추가 조건

{{예: 성능 기준, 접근성 검사, 배포 확인 절차. 없으면 "해당 없음".}}
