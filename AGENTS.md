{{프로젝트명}}은 {{프로젝트-한줄-소개}}이다.

{{프로젝트가 무엇을 하고 무엇을 목표로 하는지 두세 문장. 어떤 입력을 받아 어떤 가치를
내놓는지, 어떤 사용자를 대상으로 하는지.}}

초기 MVP는 {{MVP에서 우선 구현한 것}}을 우선 구현했고, 현재는 {{현재 단계}} 단계다.
컴포넌트별 최신 구현 현황은 `.ai/docs/00-status.md`에서 관리한다.

> 이 문서는 이 저장소의 **공통 규칙 단일 출처(single source of truth)** 다. 도구별 파일
> (`CLAUDE.md` 등)은 여기 내용을 중복하지 않고 가리키기만 한다. 규칙이 바뀌면 여기를 고친다.

---

## 1. Mission

이 저장소에서 작업하는 AI Agent는 다음 목표를 따른다.

- {{프로젝트명}}을 실제 동작 가능한 구조로 구현한다.
- 과도한 추상화보다 실행 가능한 최소 기능을 우선한다.
- {{참고할 선행 사례들}}의 장점을 참고하되 그대로 복제하지 않는다.
- {{핵심 데이터 흐름: A → B → C → D}} 흐름을 명확히 유지한다.
- 향후 {{예상되는 확장 방향}}으로 확장 가능한 구조를 유지한다.

---

## 2. MVP Scope

초기 MVP 범위는 다음과 같다. 여기 없는 것은 하지 않고, 필요하면 13절(Future Extension)에
적어 둔다.

### {{컴포넌트-A}}

- {{수행할 일 1}}
- {{수행할 일 2}}

### {{컴포넌트-B}}

- {{수행할 일 1}}
- {{수행할 일 2}}

---

## 3. Architecture Principles

각 컴포넌트가 지켜야 하는 **아키텍처 불변 원칙**만 여기 둔다. 상세 설계(내부 구조·설정 키)는
`.ai/docs/`에 두고, 여기에는 바뀌면 안 되는 큰 원칙만 적는다.

### 3.1 {{원칙 제목 — 예: 핵심 컴포넌트는 서로를 방해하지 않는다}}

- {{구체 원칙 1}}
- {{구체 원칙 2}}

### 3.2 {{원칙 제목 — 예: 저장소/외부 의존성은 교체 가능해야 한다}}

- {{구체 원칙 1}}

### 3.3 {{원칙 제목}}

- {{구체 원칙 1}}

---

## 4. Repository Structure

권장 Mono Repo 구조:

```text
{{프로젝트명}}/
├── README.md
├── AGENTS.md                # 공통 규칙 (이 파일, 단일 출처). Codex가 직접 읽는다
├── CLAUDE.md                # Claude Code 전용 안내 (이 파일을 가리킴)
├── .gitignore
├── .github/
│   ├── pull_request_template.md
│   ├── scripts/             # 문서 검사 등 CI 보조 스크립트
│   └── workflows/           # CI (문서 링크 검사 등)
├── .ai/                     # AI 에이전트 공용 (개발용)
│   ├── adr/                 # 결정 기록(ADR)
│   ├── issues/              # 오픈 이슈
│   ├── work-result/         # 작업 결과 문서 (작업 단위 기록)
│   └── docs/                # 개발 설계·현황 문서 (00-status, 컴포넌트 설계)
├── {{컴포넌트-A}}/
├── {{컴포넌트-B}}/
├── {{컴포넌트-C}}/
└── examples/                # (선택) 실행 가능한 사용 예제. 없으면 만들지 않는다
```

개발용 문서 — 컴포넌트 상세 설계, 구현 현황(`00-status`), API 설계 등 — 는 `.ai/docs/`에 두어
AI 에이전트와 기여자가 읽는다(저장소에서만 본다). 결정은 `.ai/adr/`, 이슈는 `.ai/issues/`,
작업 결과는 `.ai/work-result/`.

---

## 5. Coding Rules

### 빌드 / 실행

에이전트가 가장 먼저 필요로 하는 정보다. 명령이 바뀌면 여기를 고친다(다른 문서는 인용만 한다).

| 목적 | 명령 |
|---|---|
| 의존성 설치 | `{{설치-명령}}` |
| 빌드·테스트 | `{{빌드-명령}}` |
| 로컬 실행 | `{{실행-명령}}` |
| 린트/포맷 | `{{린트-명령}}` |

- 필요한 런타임/도구 버전은 아래 언어별 절(`{{버전 요구사항}}`, `{{빌드 도구}}`)에 적는다.
- 변경을 커밋하기 전에 최소한 `{{빌드-명령}}`은 통과시킨다. 실행하지 못했다면 그 사실과
  이유를 PR 본문과 작업 결과 문서의 **검증** 절에 적는다(통과했다고 쓰지 않는다).
- 해당 없는 항목은 지우지 말고 "해당 없음"으로 남긴다.

### 공통

- 불필요한 대규모 리팩토링 금지
- 한 번에 너무 많은 기능을 추가하지 말 것
- 동작 가능한 작은 단위로 구현
- README 또는 docs 문서와 코드가 다르면 문서를 함께 수정
- 임시 코드는 TODO를 남기되 이유를 적을 것
- 보안 정보, 토큰, 비밀번호, 개인키 커밋 금지
- 클래스와 메서드는 작게, 이름은 명확하게 짓는다(약어 지양: `RequestContext` ○ / `RC`·`Mgr`·`Util2` ✗).
  God Object와 거대 유틸리티 클래스를 피한다.
- 로깅에 비밀번호·토큰·`Authorization` 헤더·쿠키·민감 payload는 절대 남기지 않는다.

아래 언어별 절은 **그 언어를 실제로 쓸 때만** 채운다. 규칙은 프로젝트가 고른 언어·프레임워크의
관례를 따르되, 정한 것은 여기에 적어 둔다(에이전트가 추측하지 않도록).

### {{주 언어 — 예: Java}}

- 버전/런타임: {{버전 요구사항}}
- 프레임워크: {{프레임워크}}
- 빌드 도구: {{빌드 도구}}
- 모듈/패키지 이름 규칙: `{{패키지-루트}}.{{컴포넌트-A}}`, `{{패키지-루트}}.{{컴포넌트-B}}`
- {{언어별 규칙 1 — 예: 불변(immutable) DTO를 선호한다}}
- {{언어별 규칙 2 — 예: 의존성 주입은 생성자 주입을 쓰고 필드 주입을 지양한다}}

### {{프론트엔드 — 예: TypeScript}}

- 프레임워크: {{프레임워크}}
- MVP에서는 단순한 화면 우선
- 서버 응답 타입을 명확히 정의한다(계약은 `.ai/docs/07-api-design.md`).
- {{프론트엔드 규칙 — 예: UI 라이브러리는 추후 교체 가능하게 분리한다}}

### {{그 외 확장 대상 언어}}

- MVP 이후 확장 대상으로 고려
- {{고려 중인 방식}}

---

## 6. Data Model / Data Policy

{{프로젝트가 다루는 핵심 데이터의 스키마와 수집·보관 정책. 개인정보·민감정보를 다룬다면
"무엇을 수집하지 않는가"를 먼저 못 박고, 예외는 반드시 옵트인 + 기본 마스킹 + 상한으로
설계한다.}}

핵심 레코드 예시(프로젝트의 실제 스키마로 교체한다):

```json
{{핵심-레코드-예시}}
```

필드 원칙 — 값은 프로젝트에서 정하고, 정한 뒤에는 저장소 전체에서 일관되게 쓴다.
`.ai/docs/07-api-design.md`의 공통 규칙과 **반드시 같은 값**이어야 한다.

| 항목 | 이 프로젝트의 값 | 흔한 선택 |
|---|---|---|
| 시간 표현 | `{{시간-표현}}` | epoch milliseconds · ISO-8601 문자열 |
| duration 표현 | `{{duration-표현}}` | milliseconds · 초 |
| 식별자 형식 | `{{식별자-형식}}` | 정수 시퀀스 · UUID |
| 식별자 생성 주체 | `{{식별자-생성-주체}}` | 서버 · 클라이언트 · 외부 시스템 |

- 민감정보는 수집하지 않는다.
- {{수집하지 않을 것들}}

### 민감 데이터 예외 (있다면)

예외를 두어야 한다면 다음 세 가지를 항상 함께 만족시킨다. 설정하지 않으면 절대 켜지지 않는다.

1. **옵트인.** 기본은 꺼져 있다.
2. **기본 마스킹.** 켜더라도 값은 `***`로 기록되며, 실제 값을 보려면 마스킹을 따로 꺼야 한다.
   그 옵트아웃으로 노출되는 데이터에 대한 책임은 운영자가 진다.
3. **상한.** 길이·건수 상한을 두어 하나의 레코드가 비대해지지 않게 한다.

---

## 7. {{컴포넌트-A}} Design Rules

{{컴포넌트-A}}가 지켜야 하는 **불변 원칙**만 여기 둔다. 상세 설계(내부 구조·훅 지점·설정 키)는
**`.ai/docs/0N-{{컴포넌트-A}}-design.md`**, 구현 현황은 `.ai/docs/00-status.md`가 단일
출처다(여기에 중복하지 않는다).

- {{불변 원칙 1}}
- 설정은 환경변수 또는 외부 설정 파일로 주입한다(하드코딩 금지).
- {{불변 원칙 3}}

---

## 8. 인터페이스 계약

외부에 노출하는 인터페이스 계약 — 엔드포인트/스키마/명령, 요청·응답 형식, 에러 분류 — 은
**`.ai/docs/07-api-design.md`가 단일 출처**다. 여기에 초안을 중복하지 않는다.
프로토콜(REST · gRPC · GraphQL · 메시지 큐 등)이 무엇이든 그 문서 하나로 관리한다.

계약을 바꿀 때는 그 문서와 코드를 함께 고친다(문서 없이 계약 변경 금지, 12절 참고).
외부에 노출하는 인터페이스가 없는 프로젝트라면 이 절은 "해당 없음"으로 둔다.

---

## 9. Documentation Rules

문서는 한국어를 기본으로 작성한다.
단, 코드, API 명세, 필드명, 설정명은 영어를 사용한다.

### 설계 문서 형식

**`.ai/docs/`의 설계·현황 문서**는 다음 형식을 따른다. ADR·이슈·작업 결과 문서는 각자의
템플릿 형식이 따로 있으므로 여기에 해당하지 않는다.

```
# 제목

## 목적

## 범위

## 설계

## 예시

## TODO
```

설계 문서에는 반드시 다음을 포함한다.

- 왜 필요한지
- MVP에서 어디까지 할지
- 향후 확장 가능성
- 예시 설정
- 예시 요청/응답

### 문서 종류별 형식

| 문서 종류 | 위치 | 형식 |
|---|---|---|
| 설계·현황 | `.ai/docs/` | 목적/범위/설계/예시/TODO (위) |
| 결정 기록 | `.ai/adr/` | [`0000-adr-템플릿.md`](./.ai/adr/0000-adr-템플릿.md) — 배경/결정/결과 |
| 오픈 이슈 | `.ai/issues/` | [`0000-이슈-템플릿.md`](./.ai/issues/0000-이슈-템플릿.md) |
| 작업 결과 | `.ai/work-result/` | [`0000-작업결과-템플릿.md`](./.ai/work-result/0000-작업결과-템플릿.md) |

### 문서 종류별 위치

- `.ai/docs/` — **개발용 설계·현황 문서**(컴포넌트 상세 설계, API 설계, 구현 현황 등).
  저장소에서만 본다.
- `.ai/docs/00-status.md` — 컴포넌트별 **구현 현황**(상태의 단일 출처).
- `.ai/adr/` — **결정 기록(ADR).** 왜 그렇게 정했는지 배경·근거. 결정 1건 = 파일 1개.
- `.ai/issues/` — 미해결/보류 **오픈 이슈.**
- `.ai/work-result/` — **작업 결과 문서.** 작업 1건 = 파일 1개(`yyyymmdd-작업명.md`).
  무엇을 요청받아 무엇을 바꿨는지 남긴다(10절 참고).
- 규약과 새 항목 추가법은 [`.ai/README.md`](./.ai/README.md) 참고. 개발 과정의 설계·결정·이슈·
  작업 결과는 `.ai/`에 남긴다(Claude Code·Codex 공용).

---

## 10. Commit / PR Rules

### 브랜치명 / PR 제목

브랜치명과 PR 제목은 작업 Agent에 따라 다음 형식을 사용한다. PR 제목은 브랜치명과 같은 값을
사용한다.

```text
Claude Code: claude/yyyymmdd-작업명
Codex:       codex/yyyymmdd-작업명
```

- `yyyymmdd`는 브랜치를 생성한 날짜다.
- `작업명`은 한글로 작성하고, 공백 대신 하이픈(`-`)을 사용한다.
- 예: `claude/20260716-검색-기능-추가`, `codex/20260716-설정-화면-개편`

### 커밋 메시지

```
type(scope): 한글 요약
```

- `type`과 `scope`는 영문으로 작성한다.
- 요약과 상세 본문은 한글로 작성한다.

예시:

```
docs(readme): 프로젝트 개요 추가
feat({{컴포넌트-A}}): 진입점 추가
fix({{컴포넌트-B}}): 입력값 검증 오류 수정
refactor({{컴포넌트-A}}): 저장소 인터페이스 분리
```

사용 가능한 type: `docs` · `feat` · `fix` · `refactor` · `test` · `chore` · `infra`

### PR 본문 (템플릿)

- PR을 생성할 때는 저장소의 `.github/pull_request_template.md`를 본문으로 채워서 작성한다.
- GitHub 웹 UI는 이 템플릿을 자동으로 불러오지만, **AI 에이전트가 API/CLI로 PR을 만들 때는 이
  파일을 직접 읽어** 각 섹션(목적/변경 내용/검증/관련/체크리스트)을 채운 뒤 본문으로 사용한다.
- 빈 섹션을 그대로 두지 않는다 — 해당 없으면 "해당 없음"으로 표기한다. 안내용 HTML 주석
  (`<!-- ... -->`)은 값을 채우고 지운다.

### 작업 결과 문서

- 작업이 끝나면 결과를 **`.ai/work-result/0000-작업결과-템플릿.md`** 형식으로 정리해
  같은 폴더에 `yyyymmdd-작업명.md`로 남긴다(예: `20260731-작업명.md`).
- 본문(작업내용/최종정리내용)은 `.github/pull_request_template.md`의 각 섹션을 채워서 작성한다.
- `제안사항`은 없으면 "없음"으로 표기한다.

### 브랜치 정리

- **작업 브랜치는 PR 병합 후 삭제 처리한다.** 단, 환경 또는 권한 문제로 삭제할 수 없으면
  별도 안내는 하지 않고, 작업 결과 문서의 **참고사항** 영역에 그 사실을 적는다.

### 브랜치 / PR 리뷰 규칙

- **브랜치를 만들어 작업한 변경은 반드시 Pull Request로 올리고, PR 리뷰를 받은 뒤 머지한다.**
  리뷰 없이 브랜치를 `{{기본브랜치}}`에 바로 머지하지 않는다.
- 리뷰 지적사항은 반영한 뒤 해당 리뷰 스레드를 resolve하고, CI가 green인 것을 확인한 다음 머지한다.
- 예외: 오타 수정이나 지침/문서의 소규모 변경은 소유자가 명시적으로 지시할 때에 한해
  `{{기본브랜치}}`에 직접 커밋할 수 있다. 그 외 기능/버그 수정/리팩터링은 항상 리뷰된 PR을 거친다.
- 리뷰어가 없거나 자동 리뷰 할당량이 소진되면 **에이전트 간 상호 리뷰**를 한다 — 한 에이전트가
  작성한 PR은 다른 에이전트가 리뷰한다.

#### 이모지 승인 (머지 신호)

매번 채팅으로 확인하지 않아도 되도록, **소유자가 PR에 🚀(rocket) 반응을 달면 머지 승인**으로
본다. AI 에이전트는 이 신호를 확인하면 CI가 green인지 확인한 뒤 머지한다.

- 승인 신호는 **PR 본문에 단 🚀 반응** 하나다. 다른 이모지(👍 등)는 승인으로 보지 않는다
  (실수로 누른 반응이 머지를 일으키지 않도록).
- 에이전트는 반응을 직접 읽어 확인한다(GitHub API의 `reactions.rocket` 카운트).
- **주의: 에이전트는 반응 개수만 읽을 수 있고 누가 눌렀는지는 못 본다.** 그래서 🚀는 **소유자
  승인 용도로만** 쓰고, 다른 사람/봇이 🚀를 누르지 않는다는 전제로 동작한다. 이 전제가 불확실한
  PR(외부 기여자 등)에서는 이모지 대신 소유자의 명시적 코멘트/승인을 확인한다.
- 🚀가 없으면 머지하지 않는다. 되돌리기 어렵거나 논쟁 여지가 있는 변경은 이모지 승인이 있어도
  한 번 더 확인한다.

#### PR 생성 후 자동 추적 (말하지 않아도)

에이전트는 PR을 만든 뒤 **소유자가 따로 지시하지 않아도** 그 PR을 계속 추적한다. 소유자가
매번 "확인해줘 / 이모지 봤어?"라고 말할 필요가 없도록, PR을 연 것 자체를 추적 시작 신호로 삼는다.

- PR을 연 직후부터 **CI 상태·리뷰 코멘트·🚀 반응을 주기적으로 확인**한다.
- **🚀 + CI green이면 머지**한다. 리뷰 지적이 오면 반영하고, CI가 깨지면 원인을 진단해 고친 뒤
  다시 확인한다.
- 추적은 PR이 **머지 또는 close될 때까지** 계속한다. 상태 변화가 없으면 조용히 다음 확인만
  예약하고, 불필요하게 소유자를 부르지 않는다.
- 소유자가 그만하라고 하면 즉시 추적을 멈춘다.

이 규칙은 "무엇을 해야 하는가"만 정한다. **어떤 수단으로 깨어나 확인하는지는 에이전트마다
다르므로** 각 도구별 파일에 적는다(Claude Code는 [`CLAUDE.md`](./CLAUDE.md) 참고). 세션이
끝나면 추적이 끊기는 환경이라면, 끊겼다는 사실을 PR에 한 줄 남기고 소유자가 이어받게 한다.

---

## 11. AI Agent Working Rules

AI Agent는 작업 시 다음을 지킨다.

1. 먼저 현재 파일 구조를 확인한다.
2. 기존 문서와 코드 스타일을 따른다.
3. 없는 구조를 과하게 만들지 않는다.
4. 현재 범위를 벗어나는 기능은 문서의 TODO 또는 `.ai/issues/`로 남긴다.
5. 코드 생성 시 실행 가능한 최소 단위를 우선한다.
6. 설정값은 하드코딩하지 않는다.
7. 민감정보는 생성하지 않는다.
8. 변경한 파일 목록과 변경 이유를 마지막에 요약한다.
9. 테스트 또는 실행 방법을 함께 제시한다.
10. 불확실한 부분은 가정으로 명시한다.

### 11.1 AI 역할 분담

이 저장소는 **Claude Code**와 **Codex** 두 에이전트를 대상으로 한다. 별도 변경 공지가 있기
전까지 다음 소유권을 기본값으로 사용한다.
(에이전트를 하나만 쓰는 프로젝트라면 이 절은 "해당 없음"으로 둔다.)

- **Codex: 프론트엔드 담당.** `{{프론트엔드-경로}}`의 UI/UX, API 연동, 프론트엔드 테스트와
  관련 문서를 구현·관리한다.
- **Claude Code: 백엔드 담당.** `{{백엔드-경로들}}`, 실행·배포 스크립트와 해당 설계/테스트를
  구현·관리한다.
- 공용 API 계약은 백엔드가 집계·계산 책임을 갖고 `.ai/docs/07-api-design.md`에 명시한다.
  프론트엔드는 확정된 계약을 소비하며 같은 계산을 다시 구현하지 않는다.
- 다른 Agent의 기본 담당 영역을 수정해야 한다면, 사용자의 명시적 지시가 있거나 빌드/계약
  일관성을 위한 최소 변경인 경우에만 수행한다. 변경 이유와 영향 파일을 작업 결과에 남긴다.
- 양쪽이 동시에 필요한 기능은 API 계약을 먼저 합의·문서화하고, 백엔드가 계약을 제공한 뒤
  프론트엔드가 연동하는 순서를 기본으로 한다.
- 이 역할 분담은 운영 정책이다. 사용자의 최신 직접 지시가 항상 이 절보다 우선한다.

---

## 12. Do Not

다음 작업은 하지 않는다.

1. 대규모 프레임워크를 임의로 추가
2. 현재 범위를 넘는 복잡한 기능을 임의로 포함
3. {{수집/저장하지 않기로 한 데이터}}
4. 개인정보·인증 토큰·쿠키 등 민감정보 수집·로깅
5. 한 컴포넌트의 장애가 다른 컴포넌트의 요청을 실패시키는 처리
6. 불필요한 마이크로서비스 분리
7. 문서 없이 API 계약 변경
8. 임의 포트, 임의 DB 계정, 임의 비밀번호 하드코딩

---

## 13. Future Extension Ideas

현재 범위 이후 고려할 기능:

- {{확장 아이디어 1}}
- {{확장 아이디어 2}}
- {{확장 아이디어 3}}

아키텍처는 위 확장을 나중에 받아들일 수 있도록 유지한다.

---

## 14. Current Priority

현재 우선순위는 다음과 같다. **지금 무엇을 먼저 할지**만 적는다 — 진행률·완료 여부는 여기에
쓰지 않는다.

1. {{첫 구현 대상}}
2. {{다음 구현 대상}}
3. {{그 다음 구현 대상}}

"지금 실제로 무엇이 완료/진행중인지"는 이 목록을 유추하지 말고 `.ai/docs/00-status.md`를
확인한다 — 상태 정보는 그 문서 하나에서만 관리한다.

---

## 15. Definition of Done

MVP 기준 완료 조건:

1. {{완료 조건 1}}
2. {{완료 조건 2}}
3. {{완료 조건 3}}
4. README 기준 실행 방법으로 로컬 테스트 가능
