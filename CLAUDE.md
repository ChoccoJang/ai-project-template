# CLAUDE.md — Claude Code 전용 안내

> **시작 전 [`AGENTS.md`](./AGENTS.md)를 먼저 읽는다.**
>
> 이 저장소의 공통 규칙은 모두 `AGENTS.md`가 단일 출처다. 이 파일은 그 내용을 중복하지 않고,
> **Claude Code에만 해당하는 사항**만 담는다. 특히 **작업 절차의 순서와 항목은
> `AGENTS.md` 10절이 정한다** — 여기는 그 절차를 무슨 기능으로 수행하는지(수단)만 적는다.

## 먼저 볼 것

- [`AGENTS.md`](./AGENTS.md) — 공통 규칙 (필독, 단일 출처)
- [`AGENTS.md`](./AGENTS.md) 10절 — 작업 절차 체크리스트. **마지막 응답에 그대로 옮겨 적는다**
- [`.ai/project-rules.md`](./.ai/project-rules.md) — 이 프로젝트의 규칙 (배치·명령·검증)
- [`.ai/status.md`](./.ai/status.md) — 구현 현황 = 범위. 지금 무엇을 먼저 할지
- [`.ai/README.md`](./.ai/README.md) — `.ai/` 문서들의 역할과 추가 절차

## 브랜치

- **세션이 준 브랜치를 그대로 쓴다.** Claude Code는 세션마다 브랜치를 자동으로 만들어 주는데,
  그 이름이 `AGENTS.md` 8절 형식과 달라도 **재명명하지 않는다**(8절의 "도구가 만든 브랜치는
  그대로 쓴다"가 이 경우다).

## 코드 리뷰 — 10절 3번의 수단

`AGENTS.md` 8절은 "PR을 열기 전에 코드 리뷰를 한 번 돌린다"만 정하고 수단은 도구별 파일에
맡긴다. Claude Code에는 자체 기능이 있으므로 **자체 리뷰로 대신하지 않고 그 기능을 쓴다.**

- **PR을 열기 전에 `/code-review`를 한 번 돌린다.** 인증·권한·비밀정보·외부 입력을 건드린
  변경이면 `/security-review`도 함께 돌린다.
- **지적을 반영한 뒤 PR을 연다.** 반영한 것과 반영하지 않은 것(그 사유)을 PR 본문에 적는다.
- **같은 변경에 리뷰를 다시 돌리지 않는다.** 더 필요해 보이면 소유자에게 알리고 지시를
  기다린다 — 스스로 2회차를 돌리지 않는다(8절).

## PR 추적 — 10절 6·9·10번의 수단

`AGENTS.md` 10절이 "언제 걸고 언제 푸는지"를 정한다. Claude Code에서 쓸 수단은 둘이고,
**둘 다 건다.**

1. **활동 구독** — CI 실패·리뷰 코멘트가 세션으로 전달된다. `sleep`이나 반복 polling으로
   기다리지 않는다.
2. **약 1시간 뒤 예약 확인** — 웹훅으로 오지 않는 것(CI 성공, 코멘트에 달린 이모지 반응,
   머지 가능 여부)을 보는 수단이다. 구독만으로는 10절 7번의 네 가지를 다 볼 수 없다.

- **PR을 연 그 턴 안에서, 턴을 끝내기 전에 둘 다 건다**(10절 6번). "이번엔 안 걸어도
  되겠지"라는 판단 자체를 하지 않는다.
- **예약이 깨어나면 10절 7번의 순서대로 확인하고 8번대로 조치한다.** 아직 열려 있으면
  **무조건 다음 예약 확인을 걸고 턴을 끝낸다**(9번).
- **머지되거나 close되면 그 턴 안에서 구독 해제와 예약 취소까지 끝낸다**(10절 10번).
  소유자가 "닫고 구독 해제해라"라고 말해 주기를 기다리지 않는다.
- **세션이 새로 시작됐는데 이전 세션이 연 PR이 아직 열려 있으면**, 지시가 없어도 위 둘을
  다시 건다(10절 2번). 이전 세션의 예약은 죽었다고 가정한다.
