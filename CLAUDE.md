# CLAUDE.md — Claude Code 전용 안내

> **시작 전 [`AGENTS.md`](./AGENTS.md)를 먼저 읽는다.**
>
> 이 저장소의 공통 규칙은 모두 `AGENTS.md`가 단일 출처다. 이 파일은 그 내용을 중복하지 않고,
> **Claude Code에만 해당하는 사항**만 담는다.

## 먼저 볼 것

- [`AGENTS.md`](./AGENTS.md) — 공통 규칙 (필독, 단일 출처)
- [`.ai/README.md`](./.ai/README.md) — `.ai/` 문서들의 역할과 추가 절차
- [`.ai/status.md`](./.ai/status.md) — 구현 현황 = 범위. 지금 무엇을 먼저 할지

## 브랜치

- **세션이 준 브랜치를 그대로 쓴다.** Claude Code는 세션마다 브랜치를 자동으로 만들어 주는데,
  그 이름이 `AGENTS.md` 8절 형식과 달라도 **재명명하지 않는다**(8절의 "도구가 만든 브랜치는
  그대로 쓴다"가 이 경우다).

## 코드 리뷰 (수단)

`AGENTS.md` 8절은 "PR을 열기 전에 코드 리뷰를 한 번 돌린다"만 정하고 수단은 도구별 파일에
맡긴다. Claude Code에는 자체 기능이 있으므로 **자체 리뷰로 대신하지 않고 그 기능을 쓴다.**

- **PR을 열기 전에 `/code-review`를 한 번 돌린다.** 인증·권한·비밀정보·외부 입력을 건드린
  변경이면 `/security-review`도 함께 돌린다.
- **지적을 반영한 뒤 PR을 연다.** 반영한 것과 반영하지 않은 것(그 사유)을 PR 본문에 적는다.
- **같은 변경에 리뷰를 다시 돌리지 않는다.** 더 필요해 보이면 소유자에게 알리고 지시를
  기다린다 — 스스로 2회차를 돌리지 않는다(8절).
- **해결하지 못한 지적은 `.ai/issues/`에 등재하고** PR 본문 `관련`에서 그 파일을 가리킨다.

## PR 추적 (불변 규칙)

`AGENTS.md` 8절은 "PR을 연 뒤 CI·리뷰를 계속 추적한다"만 정하고 수단은 도구별 파일에
맡긴다. Claude Code에서는 아래를 **불변 규칙**으로 지킨다 — 추적을 하다 말다 하는 것을 막기
위한 규칙이므로 "이번엔 안 걸어도 되겠지"라는 판단 자체를 하지 않는다.

1. **PR을 연 그 턴 안에서, 턴을 끝내기 전에 반드시 둘 다 건다.**
   ① 그 PR의 **활동 구독**(CI 실패·리뷰 코멘트가 세션으로 전달된다 — `sleep`·반복 polling 금지),
   ② **약 1시간 뒤 예약 확인**(웹훅으로 오지 않는 CI 성공·반응·머지 가능 여부를 보는 수단).
2. **예약이 깨어나면 순서대로 확인한다:** 머지/close 여부 → CI 상태 → 리뷰 코멘트 →
   **소유자의 🚀**. 🚀는 **세 자리를 다 본다** — PR 본문의 반응, 코멘트에 달린 반응, 소유자가
   쓴 코멘트 본문(8절). 결과에 따라 조치한다(리뷰 반영, CI 수리, 🚀+green이면 머지).
3. **조치 후 PR이 아직 열려 있으면 무조건 다음 예약 확인을 걸고 턴을 끝낸다.**
   "변화 없음"은 재예약을 건너뛸 사유가 아니라 조용히 재예약할 사유다. 소유자를 부르지 않는다.
4. **멈추는 조건은 셋뿐이다: 머지됨 · close됨 · 소유자의 중지 지시.**
5. **구독이나 예약이 도구 오류로 걸리지 않으면** 재시도하고, 그래도 안 되면 그 사실을 PR에
   한 줄 코멘트로 남긴다. 추적이 조용히 끊기는 것이 최악이다. 세션이 끝나 끊기는 경우도 같다.
6. **세션이 새로 시작됐는데 이전 세션이 연 PR이 아직 열려 있으면**, 소유자의 지시가
   없어도 1번부터 다시 건다(이전 세션의 예약은 죽었다고 가정한다).
