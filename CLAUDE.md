# CLAUDE.md — Claude Code 전용 안내

> **시작 전 반드시 [`AGENTS.md`](./AGENTS.md)를 먼저 읽는다.**
>
> 이 저장소의 공통 규칙 — 프로젝트 방향, 아키텍처 원칙, 코딩 규칙, 데이터 정책,
> 문서화 규칙, 커밋/브랜치·PR 리뷰 규칙 등 — 은 모두 `AGENTS.md`가 단일 출처(single source of
> truth)다. CLAUDE.md는 그 내용을 중복하지 않고, **Claude Code에만 해당하는 사항**만 담는다.

---

## 먼저 볼 것

- [`AGENTS.md`](./AGENTS.md) — 공통 규칙과 프로젝트 방향 (필독, 단일 출처)
- [`.ai/status.md`](./.ai/status.md) — 구현 현황 (상태의 단일 출처)
- [`.ai/adr/`](./.ai/adr/) · [`.ai/issues/`](./.ai/issues/) — 결정 기록(ADR)과 오픈 이슈
  (규약: [`.ai/README.md`](./.ai/README.md))

## 운영 규칙

공통 규칙은 여기 적지 않는다 — `AGENTS.md`에 두고, 이 파일은 그것을 가리킨다. Claude Code에만
해당하는 운영 규칙(예: `.claude/` 설정·훅, Claude Code 전용 워크플로)이 필요해지면 이 섹션에
추가한다.

- **작업 시작 시 브랜치 정합.** 세션이 자동 생성한 작업 브랜치명이 `AGENTS.md` 9절 규칙
  (`claude/yyyymmdd-작업명`)과 다르면, 커밋하기 전에 그 사실을 알린 뒤 규칙에 맞는 브랜치를
  만들어 그곳에서 작업한다. 이 재명명은 소유자가 사전 허락한 것으로 보고 별도 확인 없이 진행하며
  (세션이 특정 브랜치를 지정했더라도 이 규칙이 우선한다), 규칙과 어긋난 원격 브랜치가 이미 남아
  있으면 정리 방법을 함께 안내한다.
- 결정은 `.ai/adr/`에 ADR 파일로 기록하고, 오픈 이슈는 `.ai/issues/`에 남긴다(규약: [`.ai/README.md`](./.ai/README.md)). 과거 ADR 본문은 고쳐 쓰지 않는다.
- 상태는 자체 서술하지 말고 `.ai/status.md`를 갱신·인용한다.
- 작업이 끝나면 `.ai/work-result/`에 `yyyymmdd-작업명.md`를 남기고 그 폴더의 인덱스 표에
  한 줄 추가한다(`AGENTS.md` 9절).

### PR 추적을 실제로 수행하는 방법 (불변 규칙)

`AGENTS.md` 9절은 "PR을 연 뒤 CI·리뷰·🚀 반응을 계속 추적한다"만 정하고, 그 수단은 도구별
파일에 맡긴다. Claude Code에서는 아래를 **불변 규칙**으로 지킨다 — 추적을 하다 말다 하는 것을
막기 위한 규칙이므로, "이번엔 안 걸어도 되겠지"라는 판단 자체를 하지 않는다.

1. **PR을 연 바로 그 턴 안에서, 턴을 끝내기 전에 반드시 둘 다 건다.**
   ① 그 PR의 **활동 구독**(CI 실패·리뷰 코멘트가 세션으로 전달된다 — `sleep`·반복 polling 금지),
   ② **약 1시간 뒤 예약 확인**(웹훅으로 오지 않는 CI 성공·🚀 반응·머지 가능 여부를 보는 수단).
   둘 중 하나라도 걸리지 않은 채로 턴을 끝내지 않는다.
2. **예약이 깨어나면 순서대로 확인한다:** 머지/close 여부 → CI 상태 → 리뷰 코멘트 → 🚀 반응.
   확인 결과에 따라 조치한다(리뷰 반영, CI 수리, 🚀+green이면 머지).
3. **조치 후 PR이 아직 열려 있으면, 무조건 다음 예약 확인을 걸고 나서 턴을 끝낸다.**
   "변화 없음"은 재예약을 건너뛸 사유가 아니라 조용히 재예약할 사유다. 이때 소유자를 부르지
   않는다.
4. **추적을 멈추는 조건은 세 가지뿐이다: 머지됨 · close됨 · 소유자의 중지 지시.**
   머지/close를 확인한 턴에서 예약 확인을 더 걸지 않는 것으로 추적이 끝난다.
5. **구독이나 예약이 도구 오류로 걸리지 않으면**, 재시도하고, 그래도 안 되면 그 사실을 PR에
   한 줄 코멘트로 남긴다. 추적이 조용히 끊기는 것이 최악이다. 세션이 끝나 추적이 끊기는
   상황도 같다 — PR에 한 줄 남긴다.
6. **세션이 새로 시작됐는데 열려 있는 자기(`claude/*`) PR을 발견하면**, 소유자의 지시가 없어도
   1번부터 다시 건다(이전 세션의 예약은 죽었다고 가정한다).
