# {{컴포넌트}} 설계

<!--
  이 파일은 템플릿이다. 복사해서 `{{component}}.md`(영문 소문자 슬러그)로 만들고
  `README.md` 인덱스 표에 한 줄 추가한다. 이 템플릿 파일 자체는 인덱스에 넣지 않는다.

  **덮어쓰는 문서다.** 항상 현재 모습만 담고 과거 설계를 남기지 않는다.
  "왜 그렇게 정했는가"는 `../adr/`에 ADR로 남기고 여기서 링크한다.
-->

- 갱신일: YYYY-MM-DD
- 관련 ADR: <!-- `adr/00NN-...`. 없으면 "해당 없음" -->

## 무엇을 하는가

{{이 컴포넌트가 책임지는 것 한두 문단. 책임지지 않는 것도 함께 적으면 경계가 분명해진다.}}

## 구조

{{무엇으로 이루어져 있는가. 모듈·계층·주요 타입을 목록이나 간단한 트리로.}}

```text
{{component}}/
├── {{모듈}}/     # {{역할}}
└── {{모듈}}/     # {{역할}}
```

## 흐름

{{입력이 들어와 결과가 나가기까지. 단계로 적는다.}}

1. {{단계}}
2. {{단계}}

## 인터페이스

{{바깥에 노출하는 것 — API·이벤트·설정. 입출력 형식·에러 규칙·버전 정책까지 여기 적는다.
계약을 다루는 규칙은 `AGENTS.md` 8절.}}

| 이름 | 형태 | 설명 |
|---|---|---|
| {{엔드포인트/함수}} | {{입력 → 출력}} | {{설명}} |

## 설계 결정

여기에는 **무엇을 택했는지만** 적고, 왜 그런지는 ADR에서 읽는다.

| 결정 | 근거 |
|---|---|
| {{무엇을 택했는가}} | `adr/00NN-...` |

## 알려진 한계

{{지금 구조가 못 하는 것. 고칠 계획이 있으면 `.ai/issues/`에 이슈로 두고 여기서 링크한다.}}

- {{한계}} — `.ai/issues/00NN-...`
