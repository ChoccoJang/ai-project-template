# 0N. {{컴포넌트}} 설계

<!--
  이 파일은 템플릿이다. 복사해서 `0N-{{컴포넌트}}-design.md`로 만들고
  `README.md` 인덱스 표에 한 줄 추가한다.
  AGENTS.md 9절의 문서 형식(목적/범위/설계/예시/TODO)을 따른다.
-->

## 목적

{{이 컴포넌트가 왜 필요한가. 어떤 문제를 푸는가.}}

## 범위

{{어디까지가 이 컴포넌트의 책임이고, 무엇이 아닌가. 인접 컴포넌트와의 경계를 명시한다.
현재 범위 밖이지만 언젠가 할 것은 TODO 절에 남긴다.}}

## 설계

### 구조

{{내부 구성 요소와 데이터 흐름. 필요하면 텍스트 다이어그램.}}

```text
{{입력}} → {{처리}} → {{저장/전송}}
```

### 주요 클래스 / 모듈

| 이름 | 역할 |
|---|---|
| `{{ClassName}}` | {{한 줄 설명}} |

### 설정

| 키 | 기본값 | 설명 |
|---|---|---|
| `{{설정.키}}` | `{{기본값}}` | {{무엇을 켜고 끄는지. 옵트인이면 그렇다고 명시}} |

설정은 하드코딩하지 않고 환경변수/외부 설정 파일로 주입한다(AGENTS.md 5절).

### 실패 처리

{{장애 시 어떻게 동작하는가. 무엇을 버리고 무엇을 보존하는가. 예외를 삼키는가 전파하는가.}}

## 예시

### 실행 / 설정 예시

```
{{실행 명령 또는 설정 파일 예시}}
```

### 요청 / 응답 예시

```json
{
  "example": "value"
}
```

## TODO

- {{미구현 항목 1}} — 관련 이슈 `.ai/issues/00NN-...`
- {{미구현 항목 2}}
