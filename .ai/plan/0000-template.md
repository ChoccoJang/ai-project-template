# {{계획 제목 — 무엇을 하려는지 한 줄로}}

<!--
  이 파일은 템플릿이다. 복사해 같은 폴더에 `yyyymmdd-slug.md`로 만들고, `README.md` 인덱스
  표에 한 줄 추가한다. 이 템플릿 파일 자체는 인덱스에 넣지 않는다.
  슬러그는 영문 소문자·숫자·하이픈만 쓴다. 한글 제목은 위 H1이 담는다.
  같은 작업의 작업 결과 문서와 슬러그를 맞춘다(`README.md`).
  안내용 HTML 주석은 값을 채우고 지운다.
-->

- 상태: Draft <!-- Draft | Active | Done | Dropped. 값은 이 넷뿐이고, 부연은 뒤에 괄호로 붙인다 -->
- Phase: 해당 없음 <!-- 이 계획이 속한 소분류(A-2). 속하지 않으면 "해당 없음" -->
- 관련: <!-- `.ai/status.md`의 소분류, ADR, 이슈, 작업 결과 문서. PR은 링크로 — [#NN](https://github.com/{{owner}}/{{repo}}/pull/NN). 없으면 "해당 없음" -->

## 목표

<!--
  무엇을 끝내면 이 계획이 끝나는가. 한두 문장으로 **검증 가능하게** 쓴다.
  "개선한다"가 아니라 "무엇이 무엇으로 바뀐다"로 적는다.
  진행 상황은 여기 쓰지 않는다 — `.ai/status.md`가 단일 출처다.
-->

## 손대는 것 / 손대지 않는 것

<!--
  경계를 미리 긋지 않으면 작업이 번진다. **범위라고 쓰지 않는다** — 범위는 `.ai/status.md`
  구현 현황 표의 것이고(`AGENTS.md` 2절), 여기는 그 안에서 이번에 건드릴 자리만 적는다.
-->

- 손대는 것:
- 손대지 않는 것:

## 단계

<!--
  실행 순서. 한 단계는 **그것만으로 검증되는 크기**로 자른다. 순서에 이유가 있으면
  (앞 단계가 끝나야 뒤가 가능하다) `비고`에 적는다. 단계가 8개를 넘으면 계획을 나눈다.
-->

| # | 무엇을 | 끝 조건 | 비고 |
|---|---|---|---|
| 1 | {{할 일}} | {{무엇을 확인하면 이 단계가 끝인가}} | — |
| 2 | {{할 일}} | {{끝 조건}} | 1 이후 |

## 검증

<!--
  계획 전체가 끝났다고 판단할 근거. 돌릴 명령과 봐야 할 결과를 적는다.
  명령은 `.ai/project-rules.md` 4절의 것을 그대로 쓰고 여기서 새로 만들지 않는다.
-->

-

## 전제와 위험

<!--
  틀리면 계획이 무너지는 전제, 되돌리기 어려운 단계, 막힐 수 있는 지점과 그때의 대안.
  없으면 "없음".
-->

-

## 소유자에게 물을 것

<!--
  착수 전에 소유자가 정해 줘야 하는 것. 범위 편입·우선순위·되돌리기 어려운 선택이 여기다.
  없으면 "없음"으로 적는다. 비워 두지 않는다 — 안 물은 것과 물을 게 없는 것은 다르다.
-->

없음
